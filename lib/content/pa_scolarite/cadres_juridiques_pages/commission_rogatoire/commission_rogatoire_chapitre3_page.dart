import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class PaCommissionRogatoireChapitre3Page extends StatelessWidget {
  const PaCommissionRogatoireChapitre3Page({super.key});

  static const String routeName =
      '/pa/dps_dpg/cadres_juridiques/commission_rogatoire/chapitre3';

  @override
  Widget build(BuildContext context) {
    final theme = Theme.of(context);
    final isDark = theme.brightness == Brightness.dark;

    const cardBlueAccent = Color(0xFF1565C0);

    const cardGreenAccent = Color(0xFF2E7D32);

    return const Scaffold();
  }
}
