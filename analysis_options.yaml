# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # Dossier de notes/templates pour la creation de contenu de cours
    # (pas du code Dart, juste de la documentation et des prompts)
    - docs/**
    # Code genere par Flutter / build artifacts
    - build/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    # Avertissements non-bloquants désactivés pour garder le panneau Problems propre
    deprecated_member_use: ignore
    deprecated_member_use_from_same_package: ignore
    unused_field: ignore
    unused_element: ignore
    unused_local_variable: ignore
    unused_import: ignore
    unused_element_parameter: ignore
    control_flow_in_finally: ignore
    use_build_context_synchronously: ignore
    unnecessary_import: ignore
    depend_on_referenced_packages: ignore
    library_private_types_in_public_api: ignore

linter:
  rules:
    avoid_print: false
    # Supprimés — style uniquement, trop de bruit dans la base de code générée
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
    prefer_const_declarations: false
    curly_braces_in_flow_control_structures: false
    unnecessary_const: false
    no_leading_underscores_for_local_identifiers: false
    unnecessary_brace_in_string_interps: false
    unnecessary_string_escapes: false
    unnecessary_string_interpolations: false
    unnecessary_to_list_in_spreads: false
    prefer_interpolation_to_compose_strings: false
    sort_child_properties_last: false
    use_key_in_widget_constructors: false
    use_super_parameters: false
    overridden_fields: false
    dangling_library_doc_comments: false
    prefer_final_fields: false
    file_names: false
    non_constant_identifier_names: false

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
