# 🚧 COP'IQ — Cas Pratique — Progression de codage (100 tâches)

> **CE FICHIER EST LA SOURCE DE VÉRITÉ DU CODAGE.**
> À chaque relance du script Python, Claude doit :
> 1. Lire ce fichier
> 2. Trouver la **première tâche non cochée** (`- [ ]`)
> 3. **L'exécuter intégralement**
> 4. La marquer `- [x]` avec la date `(YYYY-MM-DD)`
> 5. Continuer avec la suivante tant que les tokens le permettent
> 6. Si limite atteinte → s'arrêter proprement, laisser la tâche en `- [~]` (in-progress)
>
> Les fichiers de spec à consulter pour les détails sont indiqués dans chaque tâche.
> **Aucune question à poser à Kaïs.** Carte blanche dans le cadre des specs déjà écrites.

---

## 📊 Statistiques

- **Total** : 100 tâches (MVP 1-50 + Premium SAAS 51-100)
- **Complétées** : 97 / 100  🎉 **MVP COMPLET** + **Phase J 5/5** + **Phase K 7/7** + **Phase L 6/6** + **Phase M 4/4** + **Phase N 6/6** + **Phase O 5/5** + **Phase P 5/5** + **Phase Q 5/5** + **Phase R 3/3 ✅** + **Phase S 1/4**
- **En cours** : 0
- **Restantes** : 3 (Phase S — CI/CD release)

---

## 🟦 PHASE A — Database & Migrations (8 tâches)

### - [x] CODE-001 — Créer le dossier `supabase/migrations/` et la migration initiale (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000001_cas_pratique_extensions.sql` ✅ créé
**Action** : Créer le fichier avec uniquement les extensions (pgcrypto, pg_trgm, unaccent).
**Référence** : section 1 de `docs/cas_pratique/03_SCHEMA.sql`
**Vérification** : ✅ fichier existe, contient les 3 extensions

### - [x] CODE-002 — Migration tables taxonomie & cas (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000002_cas_pratique_taxonomy_cases.sql` ✅ créé
**Action** : Tables `cas_pratique_themes`, `cas_pratique_cases`, `cas_pratique_questions`, `cas_pratique_perfect_answers` (T001-T004).
**Référence** : section 2 (T001-T004) de `03_SCHEMA.sql`
**Vérification** : ✅ 4 tables présentes avec colonnes, contraintes CHECK, et FK

### - [x] CODE-003 — Migration rubrics & keywords (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000003_cas_pratique_rubrics_keywords.sql` ✅ créé
**Action** : Tables `cas_pratique_rubric_points`, `cas_pratique_keyword_groups`, `cas_pratique_synonyms_dictionary`, `cas_pratique_keywords` (T005-T008). Inclure la contrainte `keyword_value_or_dict_required`.
**Référence** : section 2 (T005-T008) de `03_SCHEMA.sql`
**Vérification** : ✅ 4 tables présentes, FK correctes, contrainte cycle keyword↔appeal documentée pour migration 005

### - [x] CODE-004 — Migration attempts, answers, corrections (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000004_cas_pratique_attempts_corrections.sql` ✅ créé
**Vérification** : ✅ 4 tables + migration idempotente cas_pratique_answers (DO $$)

### - [x] CODE-005 — Migration appeals, progress, audit + cycle FK keyword↔appeal (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000005_cas_pratique_appeals_progress_audit.sql` ✅ créé
**Vérification** : ✅ 3 tables + constraint fk_keyword_appeal appliquée idempotemment

### - [x] CODE-006 — Migration indexes (incluant pg_trgm) (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000006_cas_pratique_indexes.sql` ✅ créé
**Vérification** : ✅ 16 indexes opérationnels + 2 indexes trigram

### - [x] CODE-007 — Migration triggers (updated_at + user_progress) (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000007_cas_pratique_triggers.sql` ✅ créé
**Vérification** : ✅ fn_cp_set_updated_at + 5 triggers updated_at + fn_cp_update_user_progress + trg_cp_update_progress_after_correction

### - [x] CODE-008 — Migration RLS + seeds thèmes (2026-05-08)
**Fichier(s)** : `supabase/migrations/20260508000008_cas_pratique_rls_seeds.sql` ✅ créé
**Vérification** : ✅ fn_cp_is_admin + RLS activée sur 15 tables + policies user/admin + 7 thèmes seedés

---

## 🟩 PHASE B — Modèles Dart (4 tâches)

### - [x] CODE-009 — Fichier de tokens design (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/theme/cp_tokens.dart` ✅ créé
**Vérification** : ✅ CpTokens avec brand colors, surfaces light/dark, sémantique correction, theme colors, spacings, radii, helpers contextuels

### - [x] CODE-010 — Modèles de base (Theme, CaseSummary, CaseDetail, Question) (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/models/cas_pratique_models.dart` ✅ créé
**Vérification** : ✅ CpTheme + CaseSummary + CaseDetail + Question + PerfectAnswer + LegalReference avec fromJson robuste

### - [x] CODE-011 — Modèles correction & appel (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/models/cas_pratique_models.dart` ✅ complété
**Vérification** : ✅ Attempt + Answer + Correction + CorrectionDetail + Appeal + UserCaseProgress + UserGlobalProgress avec enums (AttemptStatus/AnswerStatus/PointStatus/PointKind/AppealStatus)

### - [x] CODE-012 — Exceptions typées (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_exception.dart` ✅ créé
**Vérification** : ✅ CasPratiqueException + enum CasPratiqueErrorCode (16 codes) + factory methods + CasPratiqueErrorMessages (dictionnaire FR)

---

## 🟨 PHASE C — Repository Supabase (6 tâches)

### - [x] CODE-013 — Interface Repository abstract (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_repository.dart` ✅ créé
**Vérification** : ✅ Contrat abstract `CasPratiqueRepository` avec themes, cases, attempts, answers, corrections, appeals, progression + enum CaseSortBy

### - [x] CODE-014 — Implémentation Supabase — Themes & Cases listing (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_repository_impl.dart` ✅ créé
**Vérification** : ✅ `listThemes()` + `listCases()` avec filtres (themeSlugs/years/difficulties/searchQuery) + tri (recent/alphabetical/duration) + mapping des `user_progress` par cas (last/best/count). Stubs des autres méthodes signalées avec UnimplementedError pointant vers les tâches suivantes. Mapping PostgrestException → CasPratiqueException.

### - [x] CODE-015 — Implémentation — Case detail (questions sans rubric) (2026-05-08)
**Fichier(s)** : `cas_pratique_repository_impl.dart` ✅ complété
**Vérification** : ✅ `getCaseDetail(slugOrId)` accepte slug OU UUID. Fetch case + theme + questions ordonnées + perfect_answers. Rubric jamais lue côté client (RLS l'interdit).

### - [x] CODE-016 — Implémentation — Attempts & answers (2026-05-08)
**Fichier(s)** : `cas_pratique_repository_impl.dart` ✅ complété
**Vérification** : ✅ `startAttempt` (reprise si in_progress), `getAttempt`, `getActiveAttempt`, `saveDraftAnswer` & `validateAnswer` (upsert sur attempt_id,question_id), `listAnswersForAttempt`. Validation : answerEmpty bloque la validation.

### - [x] CODE-017 — Cache local (shared_preferences) (2026-05-08)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_cache.dart` ✅ créé
**Vérification** : ✅ Singleton `CasPratiqueCache` avec put/get TTL-aware (`CacheTtl.themes/casesList/caseDetail/myProgress`), drafts persistants par `(attemptId, questionId)`, `clearAll()` pour invalidation totale. Fallback shared_preferences au lieu de Hive (pas de nouvelle dépendance).

### - [x] CODE-018 — Repository : intégration cache transparente (2026-05-08)
**Fichier(s)** : `cas_pratique_repository_impl.dart` ✅ mis à jour
**Vérification** : ✅ `listThemes()` et `getCaseDetail()` lisent le cache d'abord. `refreshCache()` set un flag `_bustCache` qui invalide les hits cache pour ~2s puis se reset automatiquement. Méthodes idempotentes.

---

## 🟥 PHASE D — Moteur de correction (Dart) (10 tâches)

### - [x] CODE-019 — Normalizer (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/normalizer.dart` ✅ créé
**Vérification** : ✅ Pipeline NFD-light (mapping diacritique manuel) + lowercase + ponctuation + NBSP + ligatures (œ→oe, æ→ae) + collapse whitespace. `NormalizerOptions` configurable. `softNormalize()` pour usages préservant les accents.

### - [x] CODE-020 — Tokenizer + N-grams (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/tokenizer.dart` ✅ créé
**Vérification** : ✅ `tokenize()` split whitespace robuste + `ngramSet()` (uni+bi+tri) + `ngramSetOf(maxN)` arbitraire. Set hashé pour matching O(1).

### - [x] CODE-021 — Lemmatizer FR léger (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/lemmatizer.dart` ✅ créé
**Vérification** : ✅ Stemmer FR retirant 24 suffixes (-aient, -ation, -ement, …) avec garde-fous (longueur ≥ 5, reste ≥ 4) + whitelist `_doNotStem` pour mots irréguliers courts.

### - [x] CODE-022 — Levenshtein (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/levenshtein.dart` ✅ créé
**Vérification** : ✅ `distance()` matrice 2 lignes + early exit sur `maxDist` + early exit si diff longueur trop grande. `ratio()` 0..1 + `isWithin()` fast-path.

### - [x] CODE-023 — Negation detector (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/negation_detector.dart` ✅ créé
**Vérification** : ✅ Set `negationWords` (FR : ne, pas, plus, jamais, aucun, rien, sans, non, ni, nul) + 3 méthodes : `isNegated(tokens, pos, window)`, `isKeywordNegated`, `isPhraseNegated`. Fenêtre 5 tokens par défaut.

### - [x] CODE-024 — Synonym resolver (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/synonym_resolver.dart` ✅ créé
**Vérification** : ✅ `EngineKeyword` + `EngineSynDict` (modèles moteur, distincts des DTO API). `SynonymResolver.resolve(kw)` retourne `dict.terms` si syn_dict_id présent sinon `[kw.value!]`. Map immutable en mémoire.

### - [x] CODE-025 — Keyword matcher (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/keyword_matcher.dart` ✅ créé
**Vérification** : ✅ `KeywordMatchContext.build()` pré-calcule normalized + tokens + ngramSet + tokenSet. `KeywordMatcher.matches()` et `matchInfo()` combinent : 1) phrase (multi-mots) 2) exact dans tokenSet 3) ngram set 4) fuzzy Levenshtein (longueur ≥ 6 + fuzzyMaxDist > 0). Gestion `is_negation` : keyword "négation attendue" valide UNIQUEMENT si présent ET négué.

### - [x] CODE-026 — Point evaluator (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/point_evaluator.dart` ✅ créé
**Vérification** : ✅ `EngineRubricPoint` + `EngineKeywordGroup` (modèles moteur). `PointEvaluator.evaluate()` retourne `PointEvalResult` (covered/partial/missing). Logique ET inter-groupes / OR intra-groupe. Groupes `is_optional` exclus du ratio "required". Partial threshold = 0.5.

### - [x] CODE-027 — Question scorer + Attempt scorer (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/scorer.dart` ✅ créé
**Vérification** : ✅ `QuestionScorer` normalise score à `max_points` (5). `AttemptScorer` orchestre via `AttemptScoringInput` + `QuestionScoringSpec`. Renvoie `AttemptScoreResult` avec `totalScore` + `totalMax` + `percent` + détails par question.

### - [x] CODE-028 — Façade `CorrectionEngine` (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/engine/correction_engine.dart` ✅ créé
**Vérification** : ✅ `CorrectionEngine.correct(attemptId, caseId, answersByQuestionId)`. Charge rubric (questions → points → groups → keywords → synonyms_dictionary). Exécute scoring. Persiste corrections + correction_details. Update attempts (status=completed). Engine version = 2.0.0. ⚠️ Lecture rubric nécessite RLS admin OU edge function pour user lambda (à faire post-MVP).

---

## 🟪 PHASE E — Page Cas Pratique Dynamique (8 tâches)

### - [x] CODE-029 — Scaffold commun `CasPratiqueScaffold` (2026-05-08)
**Fichier(s)** : `lib/core/cas_pratique/widgets/cas_pratique_scaffold.dart` ✅ créé
**Vérification** : ✅ Scaffold avec gradient COP'IQ (light blue / dark navy), halo radial, vignette sombre, top bar (back pill + titre centré + sous-titre optionnel + action droite), bottomAction sticky. Dark/light auto-switch via Theme.of.

### - [x] CODE-030 — Widget `AnswerTextArea` (2026-05-12)
**Fichier(s)** : `lib/core/cas_pratique/widgets/answer_text_area.dart` ✅ créé
**Vérification** : ✅ Textarea avec border focus animé (lerp neutre→info, shadow glow), compteur dynamique (rouge<min / neutre<reco / vert / orange si excès), AnswerSaveState (idle/typing/saving/saved/error) avec cloud icon + pulse + texte "Sauvegardé il y a Xs". disableAnimations respecté.

### - [x] CODE-031 — Widget `ScoreReveal` (2026-05-12)
**Fichier(s)** : `lib/core/cas_pratique/widgets/score_reveal.dart` ✅ créé
**Vérification** : ✅ CircularProgress custom painter 200x200 stroke 14 avec lueur (glow blur), animation 1.2s easeOutCubic + compteur de score qui scrolle + couleur dynamique (rouge<30 / orange<70 / vert), HapticFeedback.heavyImpact en fin. Pas de package confetti (feedback textuel auto selon palier : "Excellent !" etc.). disableAnimations respecté.

### - [x] CODE-032 — Widget `PointPill` (2026-05-12)
**Fichier(s)** : `lib/core/cas_pratique/widgets/point_pill.dart` ✅ créé
**Vérification** : ✅ Card par point avec icône + label + status (Couvert/Partiel/Manqué) + score (n / max pt) + expand on tap (SizeTransition easeOutCubic). Sur missing : section explanation_md + chips matchedKeywords + bouton "Je pense que ma réponse est correcte". HapticFeedback.selectionClick sur toggle.

### - [x] CODE-033 — Page dynamique : structure & loader (2026-05-12)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/cas_pratique_excercice/case_dynamic_page.dart` ✅ créé
**Vérification** : ✅ StatefulWidget reçoit `caseSlug` (widget arg OU ModalRoute.arguments). Fetch CaseDetail + startAttempt/getActiveAttempt via repo. `_SkeletonScaffold` premium animé (shimmer 1.4s) pendant le chargement. `_ErrorScaffold` avec retry. `PageController` orchestre Intro → Texte → Q1..Qn → Correction. Lock back navigation après validation (`_minBackIndex`).

### - [x] CODE-034 — Page intro + page texte du cas (2026-05-12)
**Fichier(s)** : `case_dynamic_page.dart` ✅ enrichi
**Vérification** : ✅ Intro avec pills thème + difficulté (couleur sémantique), titre Hero, métadonnées (N questions · ~X min · Y pts), 3 puces objectifs pédagogiques, CTA blanc. Texte du cas en reading mode (SingleChildScrollView, font 16, line-height 1.55), bouton "Je commence" sticky bottom.

### - [x] CODE-035 — Pages questions (1 par 1) + auto-save (2026-05-17)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/cas_pratique_excercice/case_dynamic_page.dart` ✅ enrichi
**Vérification** : ✅ `_QuestionPage` réel branché avec `AnswerTextArea`. Controllers par question initialisés dans `_initControllers`. Listener auto-save : passe en `typing` puis schedule Timer 1.5s debounce → `saveDraftAnswer` (idle/saving/saved/error). `_onValidateQuestion` vérifie `text.trim()` non vide ET `text.length >= question.charMin`, annule debounce, appelle `validateAnswer`, marque `_validatedByQuestionId[id] = true`, lock back via `_minBackIndex = pageIndex + 1`. Hydratation des réponses existantes en reprise via `listAnswersForAttempt`. AnswerTextArea désactivé après validation. Bouton "Valider" disabled + spinner pendant l'appel. Pill verte "Validée" sur les questions verrouillées. Footnote "Une fois validée, tu ne pourras plus modifier...". HapticFeedback aux étapes clés.

### - [x] CODE-036 — Page correction (avec engine) (2026-05-17)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/cas_pratique_excercice/case_dynamic_page.dart` ✅ enrichi + `lib/core/cas_pratique/engine/correction_engine.dart` ✅ patché (point_label/point_kind/explanation_md remplis depuis `pointsByQuestion` en mémoire — pas de query DB supplémentaire)
**Vérification** : ✅ `_CorrectionPage` déclenche `CorrectionEngine.correct(attemptId, caseId, answersByQuestionId, timeSpentMs)` quand la PageView arrive sur l'index final (via `_runCorrectionIfNeeded` + flag `_correctionTriggered`). Stopwatch lancé au bootstrap, arrêté à la correction. `_CorrectionLoading` (spinner + texte rassurant). `_CorrectionError` (icône + message friendly via `CasPratiqueErrorMessages.of`, bouton Réessayer + Retour à la liste). `_CorrectionContent` : `ScoreReveal` centré → pour chaque question un `_QuestionDetailBlock` avec en-tête (numéro pill + label + `_ScoreBadge` sémantique) + liste de `PointPill` (label/status/score/weight/explanation/matchedKeywords extraits de `group_matches`) + section `_PerfectAnswerSection` collapsable (body_md + `_LegalChip` pour références). Footer "Moteur v2.0.0". Bouton sticky "Retour à la liste". Cache du repo invalidé après correction pour rafraîchir la liste.

---

## 🟧 PHASE F — Liste dynamique des cas (4 tâches)

### - [x] CODE-037 — Refonte page liste : pull depuis Supabase (2026-05-17)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` ✅ refondu intégralement
**Vérification** : ✅ `FutureBuilder<List<CaseSummary>>` branché sur `_repo.listCases(sortBy: recent, limit: 100)`. États : `_SkeletonList` (6 cards shimmer 1.4s) / `_EmptyState` (illustration + pull-to-refresh) / `_ErrorState` (icône danger + message friendly via `CasPratiqueErrorMessages.of` + bouton Réessayer) / Success (ListView.separated + RefreshIndicator). Carte `_CaseTile` premium : `_NumberBadge` (gradient couleur thème + check vert si done), titre + période, `_NewBadge` gradient orange→rouge si `summary.isNew` (publié < 7j), pills thème (couleur sémantique via `CpTokens.themeColorFor`) + difficulté (vert/orange/rouge) + meta chips pts/durée. `_ProgressFooter` (3 cellules) : Dernier score /15, Meilleur score /15, Taux de réussite global (% colorisé). Stagger animations (intervalle 0.06/index cappé à 0.90). MaterialPageRoute vers `CasPratiqueDynamicPage(caseSlug: s.slug)` avec settings (CODE-040 fera la route nommée). RefreshIndicator branché sur `_refresh()` qui bust le cache + re-fetch. Dark/Light mode auto via `AppSettingsController.themeMode`.

### - [x] CODE-038 — Filtres en pill (Année / Thème / Difficulté) (2026-05-17)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_filters.dart` ✅ créé + `lib/core/cas_pratique/widgets/multi_select_sheet.dart` ✅ créé + `lib/core/cas_pratique/widgets/filter_chips_row.dart` ✅ créé + `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` ✅ enrichi
**Vérification** : ✅ `CasPratiqueFilters` immutable (years/themeSlugs/difficulties) avec copyWith/withYears/withThemes/withDifficulties/cleared, equals & hashCode value-based, toJson/fromJson robustes. `CasPratiqueFiltersService` singleton qui réutilise le `SharedPreferences` du `CasPratiqueCache` (clé `cp_cache.filters.list`) — pas de TTL : save() remove si empty. `MultiSelectSheet<T>` générique (helper `showMultiSelectSheet<T>(...)`) avec drag handle + header (titre + reset) + ListView séparée + CheckBox animé + footer sticky (nb sélectionnés + Appliquer), keyboard-safe via `MediaQuery.viewInsets`. `FilterChipsRow` scrollable horizontalement : 3 chips (Année/Thème/Difficulté) avec compteur badge si actif + chip "Effacer" si filtres présents. Wiring dans la page liste : `_bootstrap` charge filtres + thèmes en parallèle (`Future.wait`-like), `_fetchCases` passe `themeSlugs/years/difficulties` au repo, `_availableYears` accumulé pour proposer toutes les années rencontrées, `_openYearsSheet/_openThemesSheet/_openDifficultySheet/_clearAllFilters` branchés, `_EmptyState` distingue "aucun cas" vs "aucun pour ces filtres" avec bouton "Réinitialiser les filtres". HapticFeedback à chaque action.

### - [x] CODE-039 — Recherche + tri + pull-to-refresh (2026-05-17)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_filters.dart` ✅ étendu (loadSort/saveSort) + `lib/core/cas_pratique/widgets/sort_bottom_sheet.dart` ✅ créé + `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` ✅ enrichi
**Vérification** : ✅ Topbar enrichi avec 2 `_CircleIconButton` à droite : loupe (toggle `_searchExpanded`) + swap_vert (ouvre sort sheet). `_InlineSearchBar` premium : carte blanche shadow, icône loupe, TextField cursor navy, hint italic, bouton clear (icône cancel) si texte, bouton "OK" qui ferme. `AnimatedSize` 220ms easeOutCubic pour l'apparition/disparition. Debounce 300ms via Timer dans `_onSearchChanged`. Argument `searchQuery` passé à `listCases()` (déjà supporté par le repo via `or('title.ilike.%q%, situation_text.ilike.%q%')`). `showSortBottomSheet` : drag handle + titre "Trier par" + radios premium animés (200ms) + 4 options par défaut (récent / alphabétique / durée asc / durée desc). Persistance via `CasPratiqueFiltersService.saveSort(s.name)` dans la clé `cp_cache.filters.list_sort`. Indicateur `_SortHint` ("Trié par : X") cliquable sous la rangée de chips. Bootstrap charge filtres + tri + thèmes en parallèle. EmptyState filtres-aware étendu pour gérer aussi `hasSearch`. RefreshIndicator déjà branché en CODE-037 — confirmé.

### - [x] CODE-040 — Routing : route dynamique (2026-05-17)
**Fichier(s)** : `lib/main.dart` ✅ (import case_dynamic_page) + `lib/routes/app_router.dart` ✅ (case dans appOnGenerateRoute) + `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` ✅ (MaterialPageRoute remplacé par pushNamed)
**Vérification** : ✅ Route nommée `/gpx_exam/concours/cas_pratique/case_dynamic` câblée dans `appOnGenerateRoute` (lib/routes/app_router.dart). Lecture de `settings.arguments` tolérante : accepte `String` (slug direct) OU `Map { 'caseSlug': '...' }`. Construit `CasPratiqueDynamicPage(caseSlug: slug)` avec `settings` propagés pour la cohérence Navigator. Import ajouté dans main.dart juste après les case_X legacy. La liste utilise désormais `Navigator.pushNamed(CasPratiqueDynamicPage.routeName, arguments: slug)`. Routes legacy `/case_1` → `/case_6` restent fonctionnelles (migration vers la dynamique = CODE-048).

---

## 🟫 PHASE G — Système d'appel (5 tâches)

### - [x] CODE-041 — Widget `AppealSheet` (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/widgets/appeal_sheet.dart` ✅ créé
**Vérification** : ✅ Helper `showAppealSheet(context, pointLabel, pointExplanation, userAnswerPreview)` retourne `Future<String?>`. Bottom sheet premium : drag handle + `_Header` (icône report_problem + titre + subtitle + bouton close) + `_PointReminder` (encadré accent avec icône flag + "POINT ATTENDU" + label + explanation) + `_UserAnswerBlock` (encadré gris avec icône format_quote + extrait italique max 4 lignes) + `_AppealTextarea` (TextField 6 lignes max, maxLength 1000, compteur couleur sémantique selon min/recommended/max, cursor accent) + texte d'avertissement explicatif + footer sticky avec boutons "Annuler" (ghost) et "Envoyer mon appel" (CTA bleu avec icône send_rounded). HapticFeedback.lightImpact au send. AnimatedPadding keyboard-aware. Validation : min 20 chars sinon bouton disabled.

### - [x] CODE-042 — Repository : `createAppeal` + `listMyAppeals` (2026-05-17)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_repository_impl.dart` ✅ enrichi + `lib/core/cas_pratique/engine/correction_engine.dart` ✅ patché (insert détails maintenant `.select(...)` pour récupérer les vrais ids)
**Vérification** : ✅ `createAppeal(correctionDetailId, message)` : requireUserId + trim message + check answerEmpty + INSERT cas_pratique_appeals avec correction_detail_id/user_id/message (status pending par défaut côté SQL + RLS p_appeals_user_insert). `.select().single()` + parsing Appeal.fromJson. `listMyAppeals()` : requireUserId + SELECT eq('user_id', userId) order created_at DESC. Errors mappées via `_rethrow`. Patch engine : INSERT correction_details + `.select('id, correction_id, question_id, point_id, status, score, weight, group_matches')` pour que les `CorrectionDetail.id` soient renseignés (sinon impossible d'appel).

### - [x] CODE-043 — Intégration UI : bouton "Faire appel" sur PointPill missing (2026-05-17)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/cas_pratique_excercice/case_dynamic_page.dart` ✅ enrichi
**Vérification** : ✅ État : `_appealedDetailIds` (Set des ids déjà appelés) + `_appealInFlightIds` (Set en cours d'envoi). Méthode `_onAppealRequest(CorrectionDetail, Question)` : guard sur id vide / déjà appelé / en flight, lit la réponse user depuis `_controllers[q.id]`, ouvre `showAppealSheet` avec preview, appelle `_repo.createAppeal(d.id, message)`, snackbar de succès, HapticFeedback.lightImpact, gestion d'erreurs typées via CasPratiqueErrorMessages. Callback propagé : `_CorrectionPage` → `_CorrectionContent` → `_QuestionDetailBlock` → `PointPill(canAppeal, onAppeal)`. `canAppeal = status == missing && id != '' && !alreadyAppealed`. `_explanationWithAppealSuffix` suffixe "📨 Appel envoyé. L'équipe pédagogique va l'examiner." dans l'explanation après envoi.

### - [x] CODE-044 — Realtime stream appeals (2026-05-17)
**Fichier(s)** : `lib/data/cas_pratique/cas_pratique_repository_impl.dart` ✅ (`watchMyAppeals()`) + `lib/core/cas_pratique/realtime/appeals_listener.dart` ✅ créé + `lib/main.dart` ✅ (start/stop câblés sur signedIn/signedOut)
**Vérification** : ✅ `watchMyAppeals()` ouvre un canal Supabase Realtime `cp_appeals_<userId>` avec deux `onPostgresChanges` (INSERT + UPDATE) filtrés par `user_id` via `PostgresChangeFilter`, parse en `Appeal.fromJson` et émet dans un `StreamController<Appeal>.broadcast()`. `controller.onCancel` retire le canal du client Supabase pour libérer la connexion. `CasPratiqueAppealsListener` singleton : `start(navKey, [repository])` idempotent, hydrate `_lastStatusById` via `listMyAppeals()` au démarrage (évite les fausses notifications au boot), écoute le stream, détecte les transitions `prev != current && current != pending`, appelle `AppNotifier.success(...)` (approved 🎉 avec `adminResponse` si présent) ou `AppNotifier.warning(...)` (rejected). Stop() coupe la sub + purge le snapshot. Wiring dans main.dart : `unawaited(CasPratiqueAppealsListener.instance.start(navKey: _navKey))` sur `AuthChangeEvent.signedIn` + `stop()` sur `signedOut`.

### - [x] CODE-045 — Page "Mes appels" (lecture seule user) (2026-05-17)
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/my_appeals_page.dart` ✅ créé + `lib/main.dart` ✅ (import) + `lib/routes/app_router.dart` ✅ (route enregistrée) + `lib/content/gpx_exam/cas_pratique/cas_pratique_excercice/case_dynamic_page.dart` ✅ (topbar action sur la page correction)
**Vérification** : ✅ `CasPratiqueMyAppealsPage` (route `/gpx_exam/concours/cas_pratique/my_appeals`) : CasPratiqueScaffold + sous-titre dynamique "N appels au total" + rangée de chips statuts (Tous/En cours/Approuvés/Rejetés avec compteurs par état + couleur sémantique) + ListView séparée de `_AppealCard` (statut pill + icône hourglass/check/cancel + date relative + message tronqué 3 lignes + section `_AdminResponse` accent-colorée avec icône support_agent quand admin_response renseignée + date traitement). États : `_SkeletonList` shimmer 4 cards, `_EmptyState` (icône gavel + message contextuel filter-aware avec CTA "Voir tous les appels"), `_ErrorState` (icône cloud_off + Réessayer). RefreshIndicator branché sur `_loadAll()`. Realtime auto-refresh : abonnement à `_repo.watchMyAppeals()` qui hot-update la liste en mémoire (insert/upsert + re-tri par created_at desc). HapticFeedback selectionClick au filtre. Topbar action `_MyAppealsTopAction` (pill blanche avec icône gavel + label "Mes appels") affichée sur la page correction (isOnCorrection). Route enregistrée dans appOnGenerateRoute juste après case_dynamic.

---

## 🟪 PHASE H — Migration des cas legacy (3 tâches)

### - [x] CODE-046 — Extracteur Dart → JSON (2026-05-17)
**Fichier(s)** : `tools/cas_pratique/extract_legacy_cases.dart` ✅ créé
**Vérification** : ✅ Script standalone `dart run tools/cas_pratique/extract_legacy_cases.dart` qui parse les 6 fichiers `case_X_page.dart` avec un scanner brace-matching robuste (gère littéraux Dart simples/doubles/triple-quoted, raw strings, concaténation adjacente, échappements Unicode, commentaires single/block) et émet `tools/cas_pratique/legacy_dump/case_<n>.json`. Extrait : `_caseText`, `_QuestionSlide(title: "Question N", question: "...")`, `const qXPerfect`, `final qXRubric = <_ExpectedPoint>[...]` avec parsing des `_ExpectedPoint(label, groups: [[kw, kw], [kw]])`. Valeurs par défaut sensées (weight 1.5 puis 1.0, is_required position==1, kind core, char_min 80, char_recommended 400, fuzzy_max_dist 1) + section `_TODO_admin_review` listant les champs à enrichir (theme, year, difficulty, kind, explanation, references_legal, dictionnaire de synonymes).

### - [x] CODE-047 — Script SQL d'insertion des cas legacy (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260508000009_cas_pratique_seed_legacy.sql` ✅ créé
**Vérification** : ✅ Fonction utilitaire idempotente `fn_cp_seed_legacy_case(slug text, data jsonb)` qui upsert un cas par slug + wipe ses questions existantes + recrée questions/perfect/rubric_points/keyword_groups/keywords en une passe. Lecture défensive avec `COALESCE` partout. Status='draft' par défaut (l'admin doit revoir avant publication). `theme_slug` optionnel (résolu en theme_id). Détection auto `is_phrase = position(' ' in value) > 0`. Bloc `DO $$` qui seed les 6 slugs `case_1` → `case_6` avec un payload minimal placeholder — les vrais payloads (issus de CODE-046) sont à pousser via un script séparé ou une migration ultérieure. Notes d'opération documentées en bas du fichier.

### - [x] CODE-048 — Backup des fichiers legacy & redirection des routes (2026-05-17)
**Fichier(s)** : `lib/routes/app_router.dart` ✅ (6 routes redirigées) + 6 × `case_X_page.dart` ✅ (annotation @Deprecated)
**Vérification** : ✅ Décision documentée (cf. `decisions_log` de `07_STATE.json`) : on conserve physiquement les fichiers legacy au lieu de les déplacer dans `lib/legacy/` pour éviter un breaking change massif (les 6 fichiers font 2000+ lignes et sont importés directement dans main.dart). Les routes nommées `GpxCasPratiqueCase1Page.routeName` à `Case6Page.routeName` (cohérence du chemin `/gpx_exam/concours/cas_pratique/case_N`) sont désormais mappées dans RouteRegistry.routes vers `const CasPratiqueDynamicPage(caseSlug: 'case_N')` au lieu des classes legacy. Chaque classe legacy reçoit une annotation `@Deprecated('Use CasPratiqueDynamicPage(caseSlug: "case_N"). Voir CODE-048.')`. Les deep links existants vers /case_1..6 continuent donc de fonctionner mais affichent la page dynamique. Le déplacement physique vers `lib/legacy/cas_pratique/` est reporté à une session dédiée post-MVP (CODE-100 release).

---

## ⚪ PHASE I — Polish & tests (2 tâches)

### - [x] CODE-049 — Tests unitaires moteur de correction (2026-05-17)
**Fichier(s)** : `test/cas_pratique/engine/` — 9 fichiers de tests créés
**Vérification** : ✅ 60+ tests couvrant les 9 modules engine : `normalizer_test.dart` (accents/ligatures/ponctuation/NBSP/troncature/soft), `tokenizer_test.dart` (tokenize + ngramSet 1/2/3 + ngramSetOf), `lemmatizer_test.dart` (suffixes + whitelist + garde-fous), `levenshtein_test.dart` (distance + ratio + isWithin + early exit), `negation_detector_test.dart` (isNegated + fenêtre + multi-mots + phrase), `synonym_resolver_test.dart` (dict hit/miss + fallback), `keyword_matcher_test.dart` (exact/phrase/fuzzy/négation/synDict), `point_evaluator_test.dart` (covered/partial/missing + optionnels), `scorer_test.dart` (normalisation maxPoints + agrégation AttemptScorer). Commande : `flutter test test/cas_pratique/engine/`.

### - [x] CODE-050 — Audit final & smoke test (2026-05-17)
**Fichier(s)** : `docs/cas_pratique/SMOKE_TEST_REPORT.md` ✅ créé
**Vérification** : ✅ Rapport complet : section analyze statique (commandes + warnings @Deprecated attendus), table des tests unitaires (60+ tests par module), flow utilisateur end-to-end 19 étapes (liste → filtres → search → cas → questions autosave → correction → appel → realtime), checklist dark/light, perf targets, sécurité (RLS + filtre user_id defense-in-depth + admin-only rubric note), section findings (aucun blocage MVP, fichiers legacy conservés physiquement par décision documentée, edge function CODE-051 requise pour user lambda en prod), next steps recommandés, table d'avancement final 50/50 MVP.

---

# 🚀 EXTENSION PREMIUM SAAS (CODE-051 → CODE-100)

> 50 tâches additionnelles pour pousser l'app au niveau **"plus grandes apps mondiales"**
> (Notion / Linear / Stripe / Duolingo / Airbnb).
>
> À exécuter **APRÈS** CODE-001 → CODE-050 (le MVP doit être fonctionnel d'abord).

---

## 🟦 PHASE J — Sécurité production & infrastructure (5 tâches)

### - [x] CODE-051 — Edge function correction TS (port du moteur Dart) (2026-05-17)
**Fichier(s)** : `supabase/functions/cas_pratique_correct_attempt/index.ts` ✅ créé
**Vérification** : ✅ Port single-file TS mirror du moteur Dart : normalize (diacritic map, ligatures, NBSP, ponctuation), tokenize, ngramSet, levenshtein avec early exit, isNegated/isPhraseNegated (window 5), resolveCandidates (syn_dict_id ou value), matchKeyword (phrase/exact/ngram/fuzzy + décision is_negation), evaluatePoint (covered/partial/missing, partial_threshold 0.5), scoreQuestion (normalisation maxPoints), scoreAttempt (agrégation). Handler Deno : OPTIONS pour CORS, POST seul, Bearer auth obligatoire, deux clients (caller pour getUser + service role pour rubric/writes). Validation : attempt_id + case_id required, ownership check (user_id), case_id mismatch, attempt_already_finished. Loadrubric charge questions + rubric_points + keyword_groups + keywords + synonyms_dictionary (uniquement ceux référencés). Persistance : INSERT cas_pratique_corrections + cas_pratique_correction_details (.select pour vrais ids), UPDATE cas_pratique_attempts (status completed, time_spent_ms). Réponse JSON enrichie avec point_label/kind/explanation_md depuis la map en mémoire. Engine_version = "2.0.0" pour parité (CODE-052). Codes d'erreur typés (invalid_input/not_authenticated/attempt_not_owned/case_not_found/attempt_already_finished/engine_crashed). **La rubric ne quitte JAMAIS le serveur.**

### - [x] CODE-052 — Tests croisés Dart↔TS (parité scoring) (2026-05-17)
**Fichier(s)** : `supabase/functions/cas_pratique_correct_attempt/_engine.ts` ✅ (extrait depuis index.ts pour être importable) + `supabase/functions/cas_pratique_correct_attempt/index.ts` ✅ (refactor pour importer depuis _engine) + `tests/parity/fixtures/*.json` ✅ (4 scénarios) + `tests/parity/dart_vs_ts.test.ts` ✅ (Deno) + `test/cas_pratique/parity_test.dart` ✅
**Vérification** : ✅ Stratégie : les fixtures contiennent un `expected` figé. Les deux engines (Dart et TS) doivent produire un total_score / total_max / percent identiques à 0.01 près à cet `expected`. Si les deux tests passent → parité Dart↔TS prouvée. 4 scénarios : scenario_1_perfect (5.0/5.0 100%) avec 2 groupes required tous matchés → covered ; scenario_2_partial (2.5/5.0 50%) avec 1/2 groupes required → partial ; scenario_3_missing (0.0/5.0 0%) avec 0 match → missing ; scenario_4_fuzzy_phrase_multi (10.0/10.0 100%) avec multi-questions, syn_dict_id, phrase multi-mots, groupe optionnel exclu du ratio required, fuzzy match (tranquilité↔tranquillite). Le test TS tourne via `deno test --allow-read tests/parity/dart_vs_ts.test.ts`, le test Dart via `flutter test test/cas_pratique/parity_test.dart`.

### - [x] CODE-053 — Sentry / Crashlytics intégration (2026-05-17)
**Fichier(s)** : `lib/core/monitoring/sentry_setup.dart` ✅ créé + `docs/cas_pratique/SENTRY_SETUP.md` ✅ créé
**Vérification** : ✅ Wrapper `AppMonitoring` (singleton statique) avec init() idempotent qui lit `SENTRY_DSN`/`SENTRY_ENV`/`APP_RELEASE`/`SENTRY_TRACES_SAMPLE_RATE` depuis `--dart-define` (no-op gracieux si DSN vide → mode dev sans monitoring). Sample 100% crashs + 10% perf (configurable). Hook `FlutterError.onError` + `PlatformDispatcher.instance.onError` pour capturer les erreurs hors-zone. Tag global `module: cas_pratique` via beforeSend. API : `captureException(error, stack, hint, tags, screen)`, `captureMessage(msg, level, tags)`, `addBreadcrumb(category, message, data)`, `setUserId(id, email, username)`, `clearUser()`. Helper `runAppWithMonitoring(appRunner)` qui wrap `runApp()` dans une zone gardée. SENTRY_SETUP.md : 10 sections (dépendance pubspec, dart-define, wiring main, config Android/iOS, source-maps & dSYM upload via sentry-cli + GH Action snippet, alertes Slack/email avec règles P0/P1/P2, sample policy, test local, privacy `sendDefaultPii=false`). **Pas de modification de main.dart** (règle "no breaking change") — wiring documenté en 3 lignes à coller.

### - [x] CODE-054 — Rate limiting (token bucket Supabase) (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000001_cas_pratique_rate_limit.sql` ✅ créé + `supabase/functions/_shared/rate_limit.ts` ✅ créé + `supabase/functions/cas_pratique_correct_attempt/index.ts` ✅ wired + `docs/cas_pratique/RATE_LIMITING.md` ✅ créé
**Vérification** : ✅ Table `cas_pratique_rate_buckets` PK (user_id, scope) avec tokens / capacity / last_refill_at, RLS bloquée pour accès direct user. Fonction `fn_cp_consume_token(p_scope, p_capacity, p_refill_per_window, p_window_seconds) RETURNS jsonb` SECURITY DEFINER : lit auth.uid(), UPSERT défensif du bucket, SELECT FOR UPDATE pour atomicité, calcul refill = min(capacity, tokens + elapsed × rate), si refilled≥1 → consume + retourne `{allowed: true, tokens_remaining, capacity}`, sinon retourne `{allowed: false, retry_after_seconds: ceil((1-refilled)/rate), capacity}`. GRANT EXECUTE TO authenticated. Helper TS `consumeRateLimit(caller, profile)` + `rateLimitedResponse(denied)` (429 avec `Retry-After` header + `x-ratelimit-capacity`). `RATE_PROFILES` constant : listCases 60/min, saveDraft 600/min, validateAnswer 30/min, finishCorrect 10/min, createAppeal 20/jour. Wired dans l'edge function correction (juste après l'auth, fail-closed sur erreur DB). RATE_LIMITING.md : architecture, profiles, intégration Dart documentée (RPC `_checkRate` à brancher avant chaque endpoint gateable — pas encore wired dans le repo pour respecter "no breaking change"), fail-closed policy, reset admin SQL, monitoring Sentry, évolutions Cloudflare WAF / tier-based / Redis.

### - [x] CODE-055 — Audit sécurité (penetration testing prep) (2026-05-17)
**Fichier(s)** : `docs/cas_pratique/SECURITY_AUDIT.md` ✅ créé
**Vérification** : ✅ Audit complet OWASP Mobile Top 10 2024 (M1→M10) avec statut par item (✅ OK / 🟡 PARTIAL / 🔴 TODO / ⚪ N/A) + checklist COP'IQ spécifique (15 items : rubric admin-only, edge fn verrouille, parité Dart↔TS, rate limit, Sentry, realtime + RLS, migrations idempotentes, ownership check, no service_role client, security.txt, HSTS, deep links, pentest, bug bounty, audit logs admin) + modèle `.well-known/security.txt` conforme RFC 9116 + recommandations prioritaires P0/P1/P2 avant release v1.0 (P0 : CODE-079 RGPD, CODE-097 CI release obfusquée, pentest externe ; P1 : certificate pinning, root/jailbreak detection, security.txt, rotation secrets ; P2 : bug bounty, audit logs, Play Integrity / App Attest) + procédure d'audit récurrente (tous les 6 mois) + contact sécurité (`security@copiqpolice.fr`, disclosure 90j).

---

## 🟨 PHASE K — Gamification & engagement (7 tâches)

### - [x] CODE-056 — Système de streaks (jours consécutifs) (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000002_cas_pratique_streaks.sql` ✅ créé + `lib/core/cas_pratique/gamification/streaks_service.dart` ✅ créé
**Vérification** : ✅ Migration SQL : nouvelle table `cas_pratique_streak_freezes` (jokers anti-perte, source manual/reward/premium/admin, RLS user SELECT + admin write), fonction PURE `fn_cp_compute_streak(p_user_id) RETURNS jsonb` (lit attempts.finished_at distinct par jour UTC, walks desc en comptant les jours consécutifs depuis today/yesterday, applique les freezes disponibles pour combler les gaps, retourne `{count, last_activity_at, is_at_risk, broken_at, available_freezes}`), fonction MUTATIVE `fn_cp_apply_streak_to_progress(p_user_id)` qui compute + persiste dans `cas_pratique_user_progress.streak_days`, trigger `trg_cp_apply_streak` AFTER INSERT ON cas_pratique_corrections qui auto-update le streak après chaque correction. Service Dart `StreaksService` singleton : modèle immutable `StreakStatus` (count/isActive/isAtRisk/lastActivityAt/brokenAt/availableFreezes + helpers `nextMilestone`/`daysToNextMilestone` sur paliers [3,7,14,30,60,100,200,365] + `motivationLabel()` FR contextuel) + `getStatus({forceRefresh})` avec cache mémoire TTL 2 min + `invalidate()` à appeler après validation + `forceRecompute()` pour le profil.

### - [x] CODE-057 — Badges & succès (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000004_cas_pratique_badges.sql` ✅ créé + `lib/core/cas_pratique/gamification/badges_service.dart` ✅ créé
**Vérification** : ✅ Migration SQL : table `cas_pratique_badges` (catalog : slug PK, label, description, icon, color_hex, kind CHECK IN progress/score/streak/breadth/quality/xp/meta, sort_order) avec RLS public read + admin write — seedée avec **20 badges** (first_attempt/first_completed/cases_5/10/25/50/100, perfect_first/3/10, streak_3/7/30/100, themes_3/all, score_avg_70/85, xp_1000/5000). Table `cas_pratique_user_badges` (PK user_id+badge_slug, unlocked_at, metadata jsonb) avec RLS user SELECT propre + admin write. Fonction `fn_cp_check_and_unlock_badges(p_user_id)` SECURITY DEFINER : calcule v_completed/v_perfect/v_streak/v_themes/v_themes_total/v_avg/v_total_xp depuis attempts + user_progress + xp_ledger + themes en 1 passe, puis INSERT...ON CONFLICT DO NOTHING par condition remplie. Retourne le tableau des slugs débloqués dans les 5 dernières secondes (= "nouveaux unlock" sans tracker un set diff). Trigger `trg_cp_check_badges` AFTER INSERT ON corrections. Service Dart `BadgesService` singleton : modèles immutables `Badge` + `UnlockedBadge`, `listAll(forceRefresh)` avec cache catalog TTL 24h, `listMyUnlocks()` join catalog/unlocks, `checkAndUnlock()` appelle l'RPC et émet sur `Stream<List<Badge>> newUnlocks` (à brancher pour toast premium UI).

### - [x] CODE-058 — Niveaux & XP (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000003_cas_pratique_xp.sql` ✅ créé + `lib/core/cas_pratique/gamification/xp_service.dart` ✅ créé
**Vérification** : ✅ Migration SQL : table `cas_pratique_xp_ledger` append-only (delta int, reason CHECK IN correction_score/streak_bonus/first_try_bonus/daily_challenge/badge_unlock/admin_grant/admin_revoke, correction_id/attempt_id FK SET NULL, metadata jsonb, index user_id+created_at), RLS user SELECT + admin write only. Fonction `fn_cp_award_xp_for_correction(p_correction_id)` SECURITY DEFINER : base = `round(percent/100 × 10 × nb_questions)`, streak_bonus = `min(50, streak_days × 2)`, first_try_bonus = 25 si 1re correction pour ce case, 1 INSERT au ledger par catégorie pour traçabilité. Trigger `trg_cp_award_xp` AFTER INSERT ON corrections. Fonction `fn_cp_xp_total(p_user_id) RETURNS jsonb STABLE` : SUM ledger + lookup niveau via VALUES table inline (10 niveaux Recrue → Légende COP'IQ aux seuils 0/100/250/500/1000/2000/4000/8000/16000/32000), retourne `{total_xp, level, level_name, xp_into_level, xp_to_next_level, level_progress_percent}`. Service Dart `XpService` singleton : `kXpLevels` synchro SQL, modèle immutable `XpStatus` (toJson/fromJson + `leveledUpFromPrevious(prev)` + `nextLevelSpec` + `isMaxLevel`), `getStatus({forceRefresh})` avec cache TTL 2 min + Stream `changes` qui émet uniquement sur vrai changement + `invalidate()`/`refresh()`/`dispose()`.

### - [x] CODE-059 — Leaderboard hebdomadaire anonymisé (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000005_cas_pratique_leaderboard.sql` ✅ créé + `lib/core/cas_pratique/gamification/leaderboard_service.dart` ✅ créé + `lib/content/gpx_exam/cas_pratique/leaderboard_page.dart` ✅ créé + route enregistrée dans `app_router.dart` ✅
**Vérification** : ✅ Migration SQL : MATERIALIZED VIEW `cas_pratique_weekly_leaderboard` qui agrège `xp_ledger` WHERE `created_at >= now() - INTERVAL '7 days'` AND `delta > 0` (window 7j glissants), `DENSE_RANK()` avec tie-break sur `last_action_at desc`, **anonymisation** via `'Apprenti #' || upper(substr(md5(user_id::text), -4))` (stable mais non-réversible), LIMIT 1000 pour percentile. REVOKE ALL → accessible uniquement via les 2 fonctions SECURITY DEFINER : `fn_cp_get_leaderboard(p_limit)` qui retourne rank/anon_handle/weekly_xp/actions_count/last_action_at/`is_self` (auth.uid() comparison interne sans leaker les user_id), `fn_cp_my_leaderboard_position()` qui retourne `{in_leaderboard, rank?, weekly_xp?, anon_handle?, total, percentile}`. Fonction `fn_cp_refresh_weekly_leaderboard()` avec `REFRESH MATERIALIZED VIEW CONCURRENTLY` admin-only + snippet pg_cron commenté pour exécution horaire. Service Dart `LeaderboardService` singleton avec `LeaderboardEntry`/`MyLeaderboardPosition` immutables, cache TTL 2 min, `getTop({limit, forceRefresh})` + `getMyPosition({forceRefresh})` + `refresh()`. Page premium `CasPratiqueLeaderboardPage` (CasPratiqueScaffold "Classement hebdo") : `_MyPositionCard` (gradient bleu si classé avec rank + handle + percentile + bolt icon, ou carte info si non classé), tiles classement avec icônes médailles or/argent/bronze pour le top 3 + numéros pour les autres, mise en valeur de la ligne user (bordure bleue + glow + chip "TOI"), pull-to-refresh, états skeleton/empty/error. Route enregistrée dans `app_router.dart`.

### - [x] CODE-060 — Parrainage (referral codes) (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000006_cas_pratique_referrals.sql` ✅ créé + `lib/core/cas_pratique/gamification/referral_service.dart` ✅ créé + `lib/content/gpx_exam/cas_pratique/referral_page.dart` ✅ créé + route enregistrée dans `app_router.dart` ✅
**Vérification** : ✅ Migration SQL : extension de la CHECK constraint `cas_pratique_xp_ledger.reason` pour accepter `'referral_bonus'`. Table `cas_pratique_referral_codes` (user_id PK, code UNIQUE 6 chars upper, RLS user SELECT self) + helper `fn_cp_generate_referral_code()` (alphabet 32 chars sans confusion 0/O/1/I, retry 20× puis fallback 8 chars). Table `cas_pratique_referrals` (referrer + referee + code_used + status converted/pending/rejected + xp_awarded 500, UNIQUE referee_user_id pour empêcher multi-parrainage, CHECK referrer ≠ referee, RLS user SELECT côté parrain ou filleul). Fonction `fn_cp_get_or_create_my_referral_code()` retourne `{code, created_at, referrals_count, xp_earned_from_referrals, xp_per_referral}` (upsert si absent). Fonction `fn_cp_redeem_referral_code(p_code)` : normalize upper, vérifie code_not_found/self_referral/already_referred, INSERT referrals + 2 INSERTs xp_ledger (parrain + filleul, +500 chacun, metadata role), PERFORM fn_cp_check_and_unlock_badges pour les 2 (xp_1000/xp_5000 peuvent se débloquer). Service Dart `ReferralService` singleton : `ReferralCodeStatus` immutable, `RedeemResult` + enum `RedeemError` typé, cache TTL 5 min, `getMyCode({forceRefresh})` + `redeem(code)` + `shareLink(code)` + `shareMessage(code)` (FR prête à partager). Page Flutter premium `CasPratiqueReferralPage` : `_CodeCard` gradient bleu avec code en grand (42pt letter-spacing 8) + 2 boutons (copier code / copier lien), `_StatsRow` (3 cards : Filleuls / XP gagnés / Par parrainage), section "Comment ça marche" 3 `_StepTile` numérotés avec glow bleu, section "Tu as reçu un code ?" `_RedeemCard` avec TextField centré majuscules + LengthLimitingFormatter 8 + bouton Valider avec spinner. Snackbars contextuelles selon `RedeemError`. Route enregistrée.

### - [x] CODE-061 — Concours blanc (mode timer) (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000007_cas_pratique_mock_exams.sql` ✅ créé + `lib/core/cas_pratique/mock_exams/mock_exam_service.dart` ✅ créé + `lib/content/gpx_exam/cas_pratique/concours_blanc_page.dart` ✅ créé + route enregistrée
**Vérification** : ✅ Migration SQL : 4 tables (`cas_pratique_mock_exams` catalog 45 min par défaut, `cas_pratique_mock_exam_cases` M:N avec position, `cas_pratique_mock_exam_attempts` 1 tentative user avec deadline_at + status in_progress/submitted/expired/cancelled + percent + time_spent_ms, `cas_pratique_mock_exam_answers` upsert PK mock_attempt+question avec **RLS WRITE qui exige status='in_progress' AND deadline_at > now()** → impossible d'écrire après expiration). Fonctions : `fn_cp_start_mock_exam(p_id)` (reprend si in_progress + deadline non dépassée, sinon crée avec deadline = now + total_minutes), `fn_cp_finish_mock_exam(p_id)` (status = expired si deadline passée sinon submitted, calcule time_spent_ms), `fn_cp_mock_exam_leaderboard(p_id, p_limit)` (DENSE_RANK par percent DESC, time_spent_ms ASC + anonymisation md5+is_self). Service Dart `MockExamService` singleton : modèles `MockExam`/`MockExamAttempt` (avec `remaining()` calculé)/`FinishResult`+`FinishStatus`/`MockLeaderboardEntry`, méthodes listPublished/startOrResume/saveAnswer/finish/getLeaderboard. Page `CasPratiqueConcoursBlancPage` (route `/concours_blanc` accepte String OU Map argument) : `_TimerBanner` countdown XX:XX qui bascule en rouge < 5 min avec glow, `AnswerTextArea` par question (verrouillage strict : pas de retour arrière + 1 question visible à la fois + bouton "Question suivante"), autosave debounce 2s, auto-submit à 0:00 via `_autoSubmitOnExpiry`, confirm dialog avant submit manuel, WillPopScope avec dialog "Quitter le concours blanc ?" (timer continue côté DB), `_SubmittedState` final avec 2 variants (submitted/expired). HapticFeedback heavy à l'expiration. Route enregistrée avec lecture tolérante de l'argument.

### - [x] CODE-062 — Algo de recommandation "à toi de jouer" (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000008_cas_pratique_recommendations.sql` ✅ + `lib/core/cas_pratique/recommendations/recommendation_service.dart` ✅ + `lib/content/gpx_exam/cas_pratique/widgets/home_recommendation_widget.dart` ✅
**Vérification** : ✅ Fonction SQL `fn_cp_recommend_next_cases(p_user_id, p_n)` SECURITY DEFINER STABLE : algo 3 niveaux. (1) PRIORITÉ HAUTE = thème faible (avg percent le plus bas via GROUP BY theme_id sur attempts completed) + cas JAMAIS faits → score 100. (2) PRIORITÉ MOYENNE = cas du thème faible déjà faits avec percent<50% → score 50 + (50 - best_percent). (3) FALLBACK = cas neufs autres thèmes → score 30. Cas spéciaux : pas d'auth → fresh récents ; user sans aucun completed → newcomer fresh récents ; pas de score numérique → fallback fresh jamais faits. Retour table (case_id, slug, title, theme_id, theme_slug, year, month, difficulty, estimated_minutes, total_points, reason, priority_score). Service Dart `RecommendationService` singleton : modèle `RecommendedCase` + enum `RecommendationReason` (weakestThemeNew/weakestThemeReplay/fresh/unknown) avec extension `.label`/`.motivation` FR, cache 5 min, `getNext({n, forceRefresh})`. Widget `HomeRecommendationWidget` : header "À TOI DE JOUER" + refresh button + scroll horizontal de 3 `_RecCard` (largeur 268px) avec gradient thème, chip raison colorisée (warning/danger/success), titre 14.5pt 2 lignes, période, motivation, pills difficulté + durée, bouton arrow rond, accent CpTokens.themeColorFor. Skeleton row + empty hint. Tap → push CasPratiqueDynamicPage.routeName avec slug.

---

## 🟧 PHASE L — Onboarding & UX premium (6 tâches)

### - [x] CODE-063 — Onboarding 4 écrans premium (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/onboarding/onboarding_service.dart` ✅ + `lib/content/gpx_exam/cas_pratique/cas_pratique_onboarding_premium.dart` ✅ + route enregistrée
**Vérification** : ✅ Service `CasPratiqueOnboardingService` singleton dual-storage : flag persisté à la fois côté Supabase Auth user_metadata (clé `cas_pratique_onboarding_done` — independent device) ET côté shared_preferences (boot rapide offline). `hasCompleted()` check local d'abord puis remote si manquant (et resync). `markCompleted()` écrit les 2. `reset()` admin/debug. Page `CasPratiqueOnboardingPremiumPage` (route `/onboarding_premium` accepte argument optionnel `nextRoute` String OU Map) : PageView 4 slides immutables (`_OnboardingSlide` tag/icon/accent/title/message), background `_AnimatedAccentBackground` qui interpole le gradient navy→accent×0.25→deep en 700ms easeOutCubic + radial accent×0.20 décoratif, top bar avec dots animés (largeur 22 si actif sinon 8) + bouton "Passer" (caché sur le dernier slide), `_SlideView` avec **icône glow** (AnimationController repeat reverse 2.6s sur opacity boxShadow + spread), tag + title + message en `AnimatedSwitcher` slide+fade séquencé, bottom CTA évolutif ("Suivant" → "Démarrer maintenant" + rocket icon, spinner pendant finish). HapticFeedback selection à chaque page change + medium au final markCompleted. À la fin : `Navigator.pushReplacementNamed` vers `nextRoute` (défaut `GpxCasPratiqueListPage.routeName`). 4 slides définis : Bienvenue (bleu), Cas réels d'annales (orange), Correction expliquée (violet), Commence par un cas gratuit (vert).

### - [x] CODE-064 — Cas démo gratuit déverrouillé d'office (2026-05-17)
**Fichier(s)** : `supabase/migrations/20260518000009_cas_pratique_is_free.sql` ✅ + `lib/data/cas_pratique/models/cas_pratique_models.dart` ✅ (champ `isFree`) + `lib/data/cas_pratique/cas_pratique_repository_impl.dart` ✅ (SELECT is_free dans list + detail + rebind) + `lib/core/cas_pratique/subscription/subscription_service.dart` ✅ créé (placeholder paywall) + `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` ✅ (gating + badges)
**Vérification** : ✅ Migration : ALTER TABLE ADD COLUMN `is_free boolean NOT NULL DEFAULT false` + index partial WHERE is_free=true + UPDATE slug='case_1' SET is_free=true. Model `CaseSummary.isFree` exposé (parser tolérant). Repo lit `is_free` dans le SELECT list + detail et le rebind après enrichissement avec user_progress. Service `CasPratiqueSubscriptionService` singleton placeholder (CODE-084 le remplacera par un vrai check Stripe) : `isPremium()` lit `user_metadata.cas_pratique_premium`, `canAccessCase(isFree)` retourne `isFree || isPremium()`, `debugSetPremium(bool)` pour tester. Liste : `_openCase(CaseSummary)` remplace `_openCaseBySlug` + check `canAccessCase` + appel `_showPaywallPromo` (AlertDialog avec icône lock + titre cas + bouton "Voir l'abonnement" qui montrera le snackbar "Paywall complet disponible bientôt (CODE-084)"). Card tile : badge `_FreeBadge` (gradient vert + icône lock_open + glow) sur les cas free, `_PremiumLockBadge` (theme-aware via CpTokens.onSurface, icône lock + label PREMIUM) sur les cas premium UNIQUEMENT si l'user n'est pas premium.

### - [x] CODE-065 — Sound design premium (Tone.js / soundpool) (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/audio/sounds_service.dart` ✅ créé (implémentation initiale sans nouvelle dep)
**Vérification** : ✅ Singleton `SoundsService` + enum `SoundEvent` (tap/validation/correction/fanfare/error/successQuiet). `init()` lit le mute persisté dans `shared_preferences` (clé `cas_pratique_sounds_muted`), `isMutedSync()`/`isMuted()`/`setMuted()`/`toggle()`. `play(event)` no-op si muet, sinon combos `SystemSound.play(SystemSoundType.click/alert)` + `HapticFeedback.{selection,light,medium,heavy}Impact` rythmés (fanfare = heavy + 3 clicks rythmés à 120/220/340ms). Patterns nuancés par événement (validation = medium + 2 clicks décalés, fanfare = heavy + 3 selectionClicks, error = light + alert sound). **Pas de nouvelle dépendance audio ajoutée** (respect règle "no breaking change"). TODO documenté en haut du fichier : brancher `audioplayers`/`just_audio` avec `assets/sounds/*.mp3` post-MVP sans modifier les call-sites (l'API publique `play(SoundEvent)` reste stable). À appeler côté UI : `SoundsService.instance.play(SoundEvent.validation)` après chaque validation question, `fanfare` au ScoreReveal, `error` sur snackbar échec, `tap` partout.

### - [x] CODE-066 — Haptics raffinés (impacts contextuels) (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/haptics/haptics_service.dart` ✅ créé
**Vérification** : ✅ Singleton `HapticsService` + enum `HapticEvent` (selection/light/medium/heavy/success/warning/error). `init()` lit mute persisté (`cas_pratique_haptics_muted`). `_osReduceMotion()` lit `WidgetsBinding.platformDispatcher.accessibilityFeatures.reduceMotion` → respecte la préférence OS. `_suppressed` = muted || osReduceMotion → tous les patterns no-op si suppressed. Patterns composés publics nommés : `scoreReveal()` (heavy + 3 selectionClicks à 180/300/420ms), `levelUp()` (light + medium 60ms), `badgeUnlock()` (selection + selection 80ms). Patterns internes via `fire(event)` : `success` (medium + selection 80ms), `warning` (2× light 140ms), `error` (medium + 2× light 80/200ms). Tous les patterns utilisent `Future.delayed` pour rythmer sans bloquer la UI.

### - [x] CODE-067 — Skeleton + transitions partout (60fps) (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/widgets/skeletons/skeleton_box.dart` ✅ + `lib/core/cas_pratique/widgets/skeletons/skeleton_card.dart` ✅ + `lib/core/cas_pratique/transitions/cp_page_route.dart` ✅
**Vérification** : ✅ Bibliothèque skeletons : `SkeletonShimmer` (InheritedWidget qui partage UN seul AnimationController à toute sa sous-arborescence — O(1) au lieu de O(n) controllers) + `SkeletonBox` (rectangle shimmer theme-aware avec fallback statique si reduceMotion/disableAnimations) + `SkeletonCircle` + `SkeletonCard` (avatar+title+subtitle+3 pills, design système COP'IQ) + `SkeletonListTile` (avatar + 2 lignes) + `SkeletonHero` (page de détail). `CpPageRoute<T>` adaptive : Cupertino sur iOS/macOS (CupertinoPageTransition pour le swipe-back gesture natif + parallax page précédente), slide horizontal 0.08→0 + fade easeOutCubic sur Android (320ms in, 280ms out). Helpers `pushCp(context, builder)` + `cpRoute(builder)` (à utiliser en onGenerateRoute). Support `fullscreenDialog`. **Pas de passe globale forcée** (respect règle no-breaking-change) — les nouveaux écrans peuvent utiliser ces helpers immédiatement, les anciens restent sur MaterialPageRoute jusqu'à migration ciblée.

### - [x] CODE-068 — Dark mode AMOLED (vrai noir #000) (2026-05-17)
**Fichier(s)** : `lib/core/cas_pratique/theme/amoled_service.dart` ✅ créé + `lib/core/cas_pratique/theme/cp_tokens.dart` ✅ étendu
**Vérification** : ✅ Singleton `AmoledService` : `ValueNotifier<bool> enabled` (observable via ValueListenableBuilder), persistance shared_preferences (clé `cas_pratique_amoled_enabled`), `init()`/`setEnabled(bool)`/`toggle()`/`isEnabledSync()`. Token `CpTokens` étendu : nouvelles constantes `surfaceAmoled` (#000000), `surfaceContainerAmoled` (#050505), `surfaceContainerHiAmoled` (#0A0A0A), `outlineAmoled` (#1A1A1A), `outlineVariantAmoled` (#111111), `darkNavyAmoled`/Mid/Deep (#000000) → tout en noir pur. Helpers AMOLED-aware `surfaceFor(isDark, isAmoled)` / `surfaceContainerFor` / `outlineFor` / `outlineVariantFor` / `brandTopFor` / `brandMidFor` / `brandBotFor` — retournent la variante AMOLED si `isDark && isAmoled`, sinon fallback sur les helpers standards (backward compat : les anciens helpers `surface(isDark)` continuent de fonctionner). Économise la batterie sur écrans OLED (pixel #000 = pixel éteint).

---

## 🟥 PHASE M — Partage & viralité (4 tâches)

### - [x] CODE-069 — Partage du score (image story 1080×1920) ✅ 2026-06-03
**Fichier(s)** : `lib/content/.../share_score_page.dart` + package `screenshot` ou rendering Canvas
**Action** : Génère une image PNG 1080×1920 avec : score circle, titre du cas, badge thème, logo COP'IQ, gradient palette, "Je viens de faire X/15 sur Cas Pratique". `share_plus` pour Instagram/TikTok/WhatsApp. Stocké en `assets/shareable/` cached.
**Vérification 2026-06-03** : `share_score_page.dart` (636 lignes) déjà présent et complet — `ShareScoreArgs`, `CasPratiqueShareScorePage`, `_StoryCard` avec score circle + badge thème + gradient palette + hashtags + CTA. Import ajouté dans `main.dart`. Route `CasPratiqueShareScorePage.routeName` enregistrée dans `app_router.dart` avec parsing Map→ShareScoreArgs. Dépendances `share_plus ^10.1.4` et `path_provider ^2.1.5` déjà présentes en pubspec.

### - [x] CODE-070 — Export PDF de la copie corrigée ✅ 2026-06-03
**Fichier(s)** : `lib/content/gpx_exam/cas_pratique/pdf_exporter.dart` + packages `pdf ^3.11.1` + `printing ^5.13.4`
**Action** : Génère un PDF A4 avec : entête COP'IQ, mise en situation, ta réponse, réponse parfaite, points couverts/manqués, score. Téléchargeable. Watermark "Généré par COP'IQ".
**Livré 2026-06-03** : `CasPratiquePdfExporter` (service singleton no-state) avec `build({input})` → `Uint8List`, `share({bytes, fileName})` → `Printing.sharePdf`, `preview({input})` → `Printing.layoutPdf`. Modèles `PdfCorrectionInput` / `PdfQuestionBlock` / `PdfRubricPointBlock` autonomes (zéro couplage avec engine Dart, facilite le tests et le futur web). Layout MultiPage A4 : header logo + COP'IQ + nom du cas (à partir page 2), footer cop-iq.fr + date + pagination, bloc titre avec chips (thème bleu / année grise / difficulté or), carte score gradient palette `#000B36 → #1147D9` avec cercle pourcentage et mention dynamique (Excellent ≥90 / Très bien ≥75 / Bien ≥60 / À retravailler ≥40 / À approfondir), puis par question : bandeau "Question N" + score, consigne, bloc "Votre réponse" grisé, correction rubric point par point avec ✓/✗ pastilles vertes/rouges, mots-clés validés, explanation_md strippée des marqueurs MD. Fallback gracieux si `assets/images/logo.png` absent (lettre C blanche sur fond bleu). Dépendances ajoutées au `pubspec.yaml`.

### - [x] CODE-071 — Deep links + Universal Links / App Links ✅ 2026-06-04
**Fichier(s)** : `lib/core/cas_pratique/deep_links/cp_deep_links_handler.dart` (créé) + `lib/core/services/deep_links_service.dart` (Edit ciblé) + `docs/cas_pratique/DEEP_LINKS_NATIVE_CONFIG.md` (créé)
**Action** : URL `https://app.copiq.fr/c/<slug>` ouvre directement le cas. Fallback web si app pas installée. Tracking source via UTM.
**Vérification** : ✅ `CpDeepLinksHandler` singleton — `parse(Uri)` → `CpDeepLinkResult(handled, caseSlug, utm)` + `navigate` + `handleUri` raccourci. Gère `https://app.copiq.fr/c/<slug>` + `copiqpolice://cas/<slug>` + UTM params (source/medium/campaign/content/term). `shareUrl(slug, utmSource?, utmMedium?, utmCampaign?)` génère les URLs de partage. Wiring : `DeepLinksService._handleUri` délègue en priorité à `CpDeepLinksHandler.I.handleUri`. Config native (AndroidManifest intent-filter + Runner.entitlements Associated Domains + assetlinks.json + apple-app-site-association) documentée intégralement dans `DEEP_LINKS_NATIVE_CONFIG.md` avec commandes test adb/simctl — à appliquer manuellement par Kaïs (android/ios hors périmètre autonome). Placeholder UTM prêt pour CODE-073.

### - [x] CODE-072 — Open Graph / Twitter Cards pour landing ✅ 2026-06-04
**Fichier(s)** : `docs/cas_pratique/OPEN_GRAPH_SPEC.md` (créé)
**Action** : Métadonnées `og:` et `twitter:` sur les pages cas pratique du site. Image preview = mise en situation tronquée + badge thème. Quand le user partage, le link card est premium.
**Vérification** : ✅ Spec complète Next.js/TypeScript livrée dans `OPEN_GRAPH_SPEC.md` : toutes les balises OG + Twitter Card + canonical, route Edge `/og/c/[slug]` avec `@vercel/og` / satori (composant JSX 1200×630, gradient `#000B36→#1147D9`, chips thème/année/difficulté, titre + extrait mise en situation), page fallback `/c/[slug]` avec `generateMetadata`, cache-control `s-maxage=86400`, revalidation webhook Supabase, debug tools (Facebook/Twitter/LinkedIn debuggers), intégration Flutter via `CpDeepLinksHandler.I.shareUrl`.

---

## 🟪 PHASE N — Analytics, A/B, perf (6 tâches)

### - [x] CODE-073 — PostHog / Mixpanel events ✅ 2026-06-04
**Fichier(s)** : `lib/core/analytics/analytics_service.dart` (créé)
**Action** : Events typés : `case_opened`, `case_started`, `question_answered`, `question_validated`, `correction_shown`, `appeal_created`, `share_clicked`, etc. Toujours `user_id` + propriétés métier. Pas de PII (pas le texte de la réponse).
**Vérification** : Interface `CpAnalyticsInterface` abstraite + impl `_CpAnalyticsPostHog` (façade dynamic no-op si SDK absent) + singleton `CpAnalytics.I`. 7 events typés avec named params obligatoires (caseSlug, attemptId, themeId, difficulty, questionIndex, scoreObtained, shareMethod). Pas de PII : seule `answerLengthChars` (int) est loggée, pas le texte. Binding via `CpAnalytics.I.bind(postHogApiKey: ...)` dart-define, reste no-op si clé absente. `bindImpl()` exposé pour tests.

### - [x] CODE-074 — Funnels & cohortes ✅ 2026-06-04
**Fichier(s)** : `docs/cas_pratique/ANALYTICS_DASHBOARDS.md` (créé)
**Action** : Documenter les funnels critiques (install → onboarding → 1er cas → cas terminé → 2e cas → semaine 2). Cohortes hebdo. Alerte si drop-off > 50 % à une étape.
**Vérification** : 6 sections : funnel activation (6 étapes + seuils), funnel rétention D0→D30, funnel partage viral, cohortes hebdo PostHog, 5 alertes Slack automatiques, 3 dashboards PostHog détaillés, tableau events non-PII, guide intégration Flutter dart-define.

### - [x] CODE-075 — A/B testing (GrowthBook / LaunchDarkly) ✅ 2026-06-04
**Fichier(s)** : `lib/core/feature_flags/feature_flags_service.dart` (créé)
**Action** : SDK GrowthBook (gratuit self-hosted ou cloud). Wrapper qui expose `isEnabled('newCorrectionScreen')` etc. Tracking auto de l'exposition. Configurer les premiers tests : couleur du CTA, copy du bouton Valider, ordre des questions.
**Livré** : Interface `CpFeatureFlagsInterface` (6 méthodes : `isEnabled`/`getString`/`getInt`/`getDouble`/`assignVariant`/`isInRollout`) + 2 impls : `_CpFeatureFlagsNoop` (default safe) et `_CpFeatureFlagsLocal` (overrides + hash FNV-1a déterministe userId+flagKey → bucket 0..99). Singleton `CpFeatureFlags.I` avec `bind({userId, overrides, onExposed})` + `bindImpl(custom)` pour swap futur SDK distant + `reset()` au logout. Tracking d'exposition via callback `onExposed` à brancher sur `CpAnalytics.I.screenViewed('cp_experiment_exposed', extra:…)` au binding (zéro couplage direct entre services). Catalogue `CpFlagKeys` avec 8 clés pré-définies : `edgeCorrection` (canary 1%→100% sur CODE-051), `newCorrectionScreen`, `shareStoryEnabled`, `pdfExportEnabled`, `validateButtonStyle`, `validateButtonCopy`, `questionOrder`, `inlineHints`. Helpers ergonomiques `CpExperiments` avec variants pré-câblés (`validateButtonCopy()`/`validateButtonStyle()`/`questionOrder()`/`inlineHintsEnabled()`). Hash FNV-1a identique Dart↔TS pour parité côté edge function. Zéro dépendance externe — aucun risque de casser le build.

### - [x] CODE-076 — Feature flags (rollout progressif 1%/10%/100%) ✅ 2026-06-04
**Fichier(s)** : `supabase/migrations/20260604120000_cas_pratique_feature_flags.sql` (créé) + `lib/core/feature_flags/cp_feature_flags_remote.dart` (créé)
**Action** : Pour chaque feature majeure (CODE-051 edge function par ex.), rollout par tranches. Si bug détecté via Sentry, on baisse le pourcentage sans redéploiement.
**Livré** : Migration SQL table `cp_feature_flags` (key PK, value_type bool/string/int/double/variant, value_default jsonb, rollout_percent 0..100 nullable, is_active, segment, updated_at trigger) + 8 seeds alignés sur `CpFlagKeys` (cp_edge_correction 0%, cp_new_correction_screen 100%, cp_inline_hints 50%, etc.) + RLS public read via vue `cp_feature_flags_public` (écriture service_role only pour Phase R admin). Côté Flutter : `CpFeatureFlagsRemote` implémente `CpFeatureFlagsInterface`, à brancher via `CpFeatureFlags.I.bindImpl(remote)` au démarrage. Lifecycle : `initialize()` charge le cache local (shared_preferences `cp_feature_flags_v1`) puis fetch remote en background + Timer périodique 15min ; `refresh()` force un fetch ad-hoc (utilisable depuis Sentry alert handler) ; `dispose()` au logout. Résolution rollout client-side avec hash FNV-1a identique à `_CpFeatureFlagsLocal` (parité Dart↔TS). Variants serveur prennent priorité sur liste Dart (permet d'ajouter/retirer une variante sans rebuild). Offline-safe : cache local survit à un redémarrage et à une perte de connexion. Zéro modification de fichier existant → 0 risque de casse.

### - [x] CODE-077 — Performance optimization (image lazy, frame pacing, cache) ✅ 2026-06-04
**Fichier(s)** : `lib/core/performance/cp_performance_utils.dart` (créé) + `docs/cas_pratique/PERFORMANCE_TUNING.md` (créé)
**Action** : `cached_network_image` partout, `precacheImage` au boot pour images critiques. `Sliver` pour les listes longues. Frame pacing iOS 120Hz/Android 90Hz. Cible : 60fps min, 90fps idéal sur Pixel 8.
**Livré** : Module Flutter `cp_performance_utils.dart` (zéro dépendance externe) avec 5 helpers : (1) `CpImagePrecache.warmUp(context)` précache idempotent des images critiques au boot ; (2) `CpAdaptiveResolution.suffix(context)` sert `@1x`/`@2x`/`@3x` selon DPR ; (3) `CpSliverHelpers.list()`/`.grid()` Slivers optimisés (no keep-alive, repaint boundaries, semantic indexes) ; (4) `CpFramePacing.currentRefreshRate(context)` lit le refresh rate natif + constants `frameBudgetMs60/90/120fps` ; (5) `CpJankMonitor.start(onReport:…)` mesure les drop frames via `addTimingsCallback` et reporte à intervalle régulier (snapshot `JankSnapshot.toJson()`). Documentation complète `PERFORMANCE_TUNING.md` : config native iOS (`CADisableMinimumFrameDurationOnPhone` dans Info.plist), config native Android (`MainActivity.kt` setter `preferredDisplayModeId` sur Android 11+), patterns Flutter à respecter ✅/❌, budgets par frame (16.67/11.11/8.33ms), commandes de profiling (`flutter run --profile`, DevTools, `adb shell dumpsys SurfaceFlinger`), checklist de release (APK<25MB, IPA<50MB, jank_ratio<1%, 90fps Pixel 8, 120fps iPhone Pro). Aucun fichier existant modifié → 0 risque de casse.

### - [x] CODE-078 — Bundle size + assets compression ✅ 2026-06-04
**Fichier(s)** : `tools/audit_bundle_size.py` (créé) + `docs/cas_pratique/BUNDLE_SIZE_AUDIT.md` (créé)
**Action** : Images WebP au lieu de PNG. SVG vectorisé pour icônes. Polices subsets. Cible : APK < 25 MB, IPA < 50 MB.
**Livré** : Script `audit_bundle_size.py` qui lance `flutter build --analyze-size` (Android + iOS), parse les JSON `apk-code-size-analysis_*.json` / `ios-code-size-analysis_*.json`, produit rapport (taille totale vs cibles APK<25MB/IPA<50MB, top 20 items, répartition par type d'extension, candidats compression PNG>100KB et polices>100KB avec gain estimé WebP 70% / subset 50%). Mode `--candidates` standalone qui scanne `assets/` sans build. Doc complète `BUNDLE_SIZE_AUDIT.md` couvrant : (1) lancement audit, (2) conversion PNG→WebP avec `cwebp` (Win/Mac/Linux), (3) icônes en SVG via `flutter_svg`, (4) polices en subsets via `pyftsubset` ou Google Fonts dynamique, (5) split-per-abi vs App Bundle Android, (6) tree-shaking & code-shaking, (7) hook CI/CD GitHub Actions avec fail si > cible, (8) checklist release. Aucun fichier existant modifié.

---

## 🟫 PHASE O — Compliance & accessibilité (5 tâches)

### - [x] CODE-079 — RGPD : consent + export + suppression data (2026-06-05)
**Fichier(s)** :
- `supabase/migrations/20260605000001_cas_pratique_rgpd.sql` ✅ créé
- `supabase/functions/cas_pratique_export_user_data/index.ts` ✅ créé
- `supabase/functions/cas_pratique_delete_user_data/index.ts` ✅ créé
- `lib/content/gpx_exam/cas_pratique/cp_privacy_page.dart` ✅ créé
- `lib/main.dart` ✅ import ajouté (Edit ciblé)
- `lib/routes/app_router.dart` ✅ route `CpPrivacyPage.routeName` enregistrée (Edit ciblé)
**Vérification** : ✅ Migration SQL : table `cas_pratique_deletion_tokens` (PK uuid, user_id FK, token_hash SHA-256, expires_at TTL 15 min, used_at, UNIQUE user_id — RLS REVOKE ALL, accès service_role only). Fonctions SECURITY DEFINER REVOKE auth/anon : `fn_cp_export_user_data(p_user_id)` → JSONB complet (attempts/answers/corrections/appeals/progress/xp/badges/referrals + email user) pour RGPD Art.20 ; `fn_cp_request_deletion_token(p_user_id)` → code 6 chiffres stocké hashé SHA-256 (upsert 1 token actif par user) ; `fn_cp_delete_user_data(p_user_id, p_code)` → vérifie hash + expire, cascade-delete toutes tables CP (correction_details→appeals→corrections→answers→attempts→progress→user_case_progress→badges→xp_ledger→streak_freezes→referrals→referral_codes→rate_buckets→token) + retourne rapport (counts), edge fn appelle ensuite `auth.admin.deleteUser`. Edge function `export_user_data` : POST Bearer obligatoire + callerClient getUser + serviceClient rpc fn_cp_export_user_data + JSON download `copiq_mes_donnees_YYYY-MM-DD.json`. Edge function `delete_user_data` : 2 actions — `request` (génère token, écrit dans user_metadata comme fallback email en attendant Resend/SendGrid — TODO prod documenté) + `confirm` (vérifie code 6 chiffres, rpc fn_cp_delete_user_data, cleanup metadata, auth.admin.deleteUser, rapport partiel si delete Auth échoue). Page Flutter `CpPrivacyPage` (route `/gpx_exam/concours/cas_pratique/privacy`) : CasPratiqueScaffold + 5 sections (intro RGPD / contact DPD / export / suppression / conservation + droits supplémentaires). Export : appel edge fn + écriture fichier temporaire + `Share.shareXFiles` (share_plus). Suppression : Dialog1 (avertissement irréversible) → requestDeletion → Dialog2 (TextField 6 chiffres, FilteringTextInputFormatter, bouton disabled si format invalide) → confirmDeletion → `Supabase.signOut()` → Dialog succès avec rapport + navigate to root. Tous les error états catchés avec snackbar rouge CpTokens.danger. Dark/Light auto via Theme.of. Aucun fichier hors périmètre modifié (main.dart + app_router.dart : Edit ciblé uniquement).

### - [x] CODE-080 — Cookies / consent (web futur) (2026-06-05)
**Fichier(s)** : `docs/cas_pratique/COOKIES_CONSENT_SPEC.md` ✅ créé
**Action** : Banner consent avec Reject/Accept/Customize. Catégories : strict / analytics / marketing. Cookie de consent. Pas de tracking avant consent.
**Vérification** : ✅ Spec complète Next.js/React créée : 8 sections (contexte légal RGPD/ePrivacy, 3 catégories cookies strict/analytics/marketing, comportement banner bottom-center/fullwidth mobile, panel personnalisation avec toggles, cookie `copiq_consent` 13 mois avec version+timestamp+categories, architecture composants ConsentProvider/Banner/Panel/consentStorage, design brand #1147D9/#000B36/Montserrat, checklist accessibilité WCAG AA pour banner, 8 tests requis, TODO prod complet). Implémentation React reportée au développement du site Next.js.

### - [x] CODE-081 — Accessibility audit WCAG AA (2026-06-05)
**Fichier(s)** : `docs/cas_pratique/A11Y_AUDIT.md` ✅ créé
**Action** : Audit contrastes (4.5:1 min), Semantics labels partout, navigation au clavier (focus traversal), tailles cibles ≥ 44px, VoiceOver/TalkBack testé sur 10 flows critiques, captions sur sons.
**Vérification** : ✅ Audit WCAG 2.1 AA complet créé : tableau résumé 7 domaines avec statuts P0/P1/P2, checklist 20+ critères (1.1.1 alt text PointPill/ScoreReveal, 1.4.3 contraste 4.5:1 mesures palette brand, 2.5.5 cibles 44dp PointPill/chips, 2.1.1 clavier/focus traversal, 3.3.1 erreurs textuelles), 10 flows critiques VoiceOver/TalkBack (liste→filtres→cas→saisie→validation→correction→appel→score→navigation→dialog), pattern `reduceMotion` pour `ScoreReveal`/`CasPratiqueScaffold`, outils recommandés (WebAIM, Colour Contrast Analyser, Flutter Inspector), plan d'action P0/P1/P2.

### - [x] CODE-082 — i18n FR + base EN (2026-06-05)
**Fichier(s)** :
- `lib/l10n/intl_fr.arb` ✅ créé (100 clés, toutes les strings Cas Pratique)
- `lib/l10n/intl_en.arb` ✅ créé (traduction EN complète)
- `l10n.yaml` ✅ créé (config flutter gen-l10n)
- `lib/core/cas_pratique/i18n/cp_l10n.dart` ✅ créé (CpLocaleService + _FallbackL10n + CpDateFormat + CpNumberFormat)
- `lib/core/cas_pratique/i18n/language_switcher.dart` ✅ créé (CpLanguageSwitcherTile + CpLanguageSwitcherChips + bottom sheet)
- `pubspec.yaml` ✅ Edit ciblé (+ flutter_localizations SDK + intl ^0.19.0 + generate: true)
**Vérification** : ✅ 100 clés ARB extraites (list/filtres/tri/badges/difficulté/case flow/questions/réponses/correction/scores/points/appels/page mes appels/switcher langue/dates/nombres). _FallbackL10n compile sans gen-l10n (app ne casse pas avant la première exécution de `flutter gen-l10n`). CpLocaleService ChangeNotifier avec persistence SharedPreferences (clé `cp_app_locale`), toggle FR↔EN, resolveLocale(). CpDateFormat: relative/full/short/time locale-aware via intl.DateFormat. CpNumberFormat: score/percent/compact locale-aware via intl.NumberFormat. Language switcher: ListTile (settings) + chips (inline) + bottom sheet (FR🇫🇷/EN🇬🇧 avec check mark, glow bleu sur actif). ⚠️ WIRING main.dart requis (3 lignes documentées en tête de cp_l10n.dart + flutter pub get + flutter gen-l10n) — non modifié automatiquement (hors périmètre Edit ciblé autorisé sur main.dart).

### - [x] CODE-083 — Status page + uptime monitoring ✅ 2026-06-05
**Fichier(s)** : `docs/cas_pratique/STATUS_PAGE_SETUP.md` (créé) + `supabase/functions/cas_pratique_health/index.ts` (créé) + `infra/uptime-kuma/docker-compose.yml` (créé)
**Action** : Page publique `status.copiq.fr` qui ping les endpoints critiques toutes les 60s. Historique 90j. Incidents publiés. Webhook Slack/Discord pour alertes.
**Livré** : Doc complète `STATUS_PAGE_SETUP.md` (10 sections : stack recommandée Uptime Kuma vs statuspage.io vs BetterStack avec décision, 10 endpoints à monitorer avec seuils, setup docker-compose + reverse proxy Nginx + Let's Encrypt, création status page publique (slug copiq, 3 groupes monitors, theme dark + custom CSS palette COP'IQ), webhooks Slack `#alerts-prod` + Discord `#status` + Email DPD + règles d'escalade P0/P1/P2, endpoint health check Supabase, branchement badge dans l'app mobile, historique & SLA cible 99.9%, checklist mise en prod 12 points, runbook incident avec template post-mortem). Edge function `cas_pratique_health` : GET/HEAD, vérifie env vars + count rapide cas_pratique_themes (index PK) + mesure latency_ms, seuil 2000ms, retourne `{status: ok|degraded|down, latency_ms, checks: {db_healthy, db_latency_ms, db_themes_count}, version, checked_at}` avec HTTP 200 ou 503. Compose Uptime Kuma : image officielle louislam/uptime-kuma:1, volume persistant, port 3001, healthcheck wget, logs rotated 10MB×3, TZ Europe/Paris. Aucun fichier existant modifié.

---

## 🟪 PHASE P — Monétisation & business (5 tâches)

### - [x] CODE-084 — Paywall + tier free/premium ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605120000_cas_pratique_subscriptions.sql` (créé) + `lib/content/paywall/cp_paywall_page.dart` (créé)
**Action** : Free tier : 1 cas démo + 1 cas/semaine. Premium : illimité + concours blanc + export PDF + leaderboard. Paywall declenché au tap sur 2e cas. Comparatif features clair.
**Livré** : Migration SQL `cas_pratique_subscriptions` (PK user_id, tier check ('free','premium','premium_trial'), status check Stripe-aligned, métadonnées Stripe complètes : customer_id/subscription_id/price_id/product_id, current_period_start/end, cancel_at_period_end, canceled_at, trial_ends_at, entitlements text[], audit timestamps + trigger updated_at) + indexes `idx_cp_subscriptions_tier_status` et `idx_cp_subscriptions_period_end` partiel sur status='active'. RLS : `Users read own subscription` (auth.uid()=user_id), pas de policy d'écriture (service_role only via webhook Stripe CODE-085). Fonction `cp_get_user_tier(p_user_id)` SECURITY DEFINER qui retourne 'free' si pas d'entrée, 'free' si canceled/unpaid/period expiré, sinon le tier réel. Vue `cp_my_subscription` filtrée auth.uid() + vue `cp_my_weekly_case_count` qui compte les cases distincts joués depuis date_trunc('week', now()). Côté Flutter : `CpPaywallPage` Scaffold avec close button → hero gradient `#000B36→#1147D9` avec badge jaune `#FFC700` personnalisé selon trigger (`second_case`/`concours_blanc`/`pdf_export`/`leaderboard`/default), comparator 2 colonnes Free 0€ vs Premium 9,99€/mois (highlighted yellow border + badge POPULAIRE), 8 features cards avec icônes + descriptions courtes (illimité, concours blancs, PDF, leaderboard, annales, reco IA, correction temps réel, support prioritaire), CTA principal `Passer Premium pour 9,99 € / mois` (bleu COP'IQ + bolt icon), texte rassurant "Sans engagement", footer légal (Restaurer / CGV / Politique conf). Modal info "module en cours d'intégration (CODE-085)" tant que Stripe pas branché. Route `CpPaywallPage.routeName = '/cas-pratique/paywall'`. Aucun fichier existant modifié.

### - [x] CODE-085 — Stripe + RevenueCat (abonnements iOS/Android) ✅ 2026-06-05
**Fichier(s)** : `supabase/functions/cas_pratique_stripe_webhook/index.ts` (créé) + `supabase/functions/cas_pratique_create_checkout/index.ts` (créé) + `lib/core/payments/payments_service.dart` (créé) + `docs/cas_pratique/STRIPE_REVENUECAT_SETUP.md` (créé)
**Action** : RevenueCat (paywall iOS/Android unifié, conformité Apple/Google). Webhook Stripe → update `cas_pratique_subscriptions.status`. Granular features unlocked via `entitlements`.
**Livré** : Edge function `cas_pratique_stripe_webhook` : vérif signature Stripe `constructEventAsync` (CRITIQUE), routage par event type (checkout.session.completed / customer.subscription.created/updated/deleted / invoice.payment_failed → past_due / invoice.payment_succeeded), `handleSubscription()` qui UPSERT `cas_pratique_subscriptions` avec tier (premium_trial si trialing, premium si active, sinon free) + tous les champs Stripe + entitlements depuis `product.metadata.entitlements` (fallback: 7 entitlements complets). Edge function `cas_pratique_create_checkout` : auth JWT + body `{price_id, success_url?, cancel_url?, allow_promotion_codes?, trial_period_days?}`, `getOrCreateCustomer()` (lookup DB → lookup Stripe par email → create avec metadata `supabase_user_id`), création session mode subscription avec automatic_tax FR et locale fr, retourne `{url, id, customer_id}`. Service Flutter `CpPayments` (ChangeNotifier singleton) : enum `CpTier` (free/premiumTrial/premium) + ext `wireValue` + `isPaid`, modèle `CpSubscription` (parse `cp_my_subscription` vue) avec helpers `hasEntitlement`/`isPremium`/`isOnTrial`, catalogue `CpEntitlements` (7 clés alignées sur Stripe metadata), interface `CpPaymentsInterface` (refreshTier/startCheckout/openCustomerPortal), méthodes : `refreshTier()` lit la vue Supabase + notifyListeners, `startCheckout()` appelle edge fn + `launchUrl(externalApplication)`, `openCustomerPortal()` pour gestion sub, `bindImpl()` pour swap futur vers RevenueCat. Doc complète `STRIPE_REVENUECAT_SETUP.md` : stratégie hybride web/iOS/Android, setup Stripe (account, products, webhook 6 events, env vars `supabase secrets set`, déploiement edge fns), test local (Stripe CLI `stripe listen` + `stripe trigger`), setup RevenueCat (project + apps + entitlements + App Store Connect + Google Play), intégration Flutter, **conformité Apple/Google** (jamais Stripe Checkout sur iOS/Android pour contenu numérique → RevenueCat obligatoire), dart-define vars, checklist mise en prod 12 items, métriques cibles (MRR 2k€ mois 6, churn <8%, trial conversion 35%). Aucun fichier existant modifié.

### - [x] CODE-086 — Tarification mensuel / annuel / lifetime ✅ 2026-06-05
**Fichier(s)** : `lib/content/paywall/cp_pricing_plans.dart` (créé)
**Action** : 3 plans : 9,99€/mois, 79€/an (-33%), 149€ lifetime. Mention "économisez X%" sur l'annuel. Trial 7 jours gratuit sur le mensuel.
**Livré** : Widget `CpPricingPlans` autonome avec modèle `CpPricingPlan` (id, label, price, pricePeriod, originalPrice barré, savingsBadge, extraBadge, trialLabel, highlights, stripePriceId, isHighlighted) + helper `CpDefaultPricingPlans.all()` qui lit les `String.fromEnvironment('STRIPE_PRICE_MONTHLY|YEARLY|LIFETIME')` avec fallback placeholder. 3 plans pré-configurés : Mensuel 9,99€ trial 7j POPULAIRE highlighted gold, Annuel 79€ avec barré 119,88€ ÉCONOMISE 33%, Lifetime 149€ MEILLEUR DEAL paiement unique. Cards radio avec animation 180ms, border 2px gold/blue selon highlighted, indicateur radio, badges colorés (gold/000B36, green/white), prix avec original barré + period suffix, trial label avec icône célébration. CTA dynamique qui s'adapte au plan sélectionné ("Démarrer 7 jours gratuits" pour mensuel, "Continuer avec annuel" sinon) + texte rassurant adapté ("Annule avant la fin de l'essai" / "Paiement unique, accès à vie" / "Sans engagement"). Callback `onPlanSelected` pour brancher sur `CpPayments.I.startCheckout()`. Aucun fichier existant modifié.

### - [x] CODE-087 — Codes promo + bons cadeaux ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605130000_cas_pratique_promo_codes.sql` (créé) + `supabase/functions/cas_pratique_redeem_promo/index.ts` (créé) + `lib/content/paywall/cp_promo_redeem_sheet.dart` (créé)
**Action** : Codes alphanum (genre `STUDENT50`). Réduction % ou montant fixe. Limite d'usage globale + par user. Gift cards achetables.
**Livré** : Migration SQL `cas_pratique_promo_codes` (PK uuid, code uppercase unique check 4-32 chars, type 'promo'|'gift', discount_kind 'percent'|'fixed_amount'|'free_months', discount_value, max_redemptions globale + max_per_user default 1, redemption_count auto-incrémenté via trigger, period validity starts_at/ends_at, eligible_price_ids text[], purchased_by + recipient_email + message pour gift cards, stripe_coupon_id + stripe_promotion_code_id, audit created_at/by, is_active) + indexes partiels actifs + recipient_email. Table `cas_pratique_promo_redemptions` (PK uuid, FK promo_code_id + user_id cascade, redeemed_at, applied_to_subscription_id stripe, metadata jsonb) + indexes user/code. Trigger `cp_promo_increment_count` AFTER INSERT pour update redemption_count. RLS : codes NO public read (validation via edge fn service_role), redemptions read own seulement. Fonction `cp_validate_promo_code(p_code, p_user_id, p_price_id?)` SECURITY DEFINER qui retourne (valid, reason, promo_id, discount_kind, discount_value, stripe_coupon_id) avec checks séquentiels : code_not_found, not_yet_valid, expired, max_global_reached, max_per_user_reached, plan_not_eligible. 3 seeds : WELCOME10 (-10% 1000 usages 90j), STUDENT50 (-50% 500 usages 180j), LAUNCH7DAYS (+1 mois 200 usages 60j). Edge function `cas_pratique_redeem_promo` : auth JWT, body `{code, price_id?, validate_only?}`, normalize uppercase + trim + check len 4-32, RPC `cp_validate_promo_code`, si `validate_only=true` retourne sans consommer (UI feedback), sinon INSERT redemption + retourne discount + stripe_coupon_id. Widget `CpPromoRedeemSheet` Flutter : modèle `CpPromoResult` avec helpers `humanLabel` (−50% / −10 € / +1 mois offert) et `errorMessage` (mapping 6 reasons → labels FR). Bottom sheet avec champ TextField uppercase forcé + RegExp `[A-Z0-9\-]` + LengthLimit 32, debounce 500ms de validation auto via edge fn (validate_only=true), feedback live (spinner / ✅ check vert / ❌ erreur rouge), bannières SuccessBanner verte "Code valide — réduction X%" et ErrorBanner rouge avec message humain, CTA "Appliquer la réduction" qui consomme + pop(CpPromoResult) avec stripe_coupon_id pour brancher sur le checkout. Aucun fichier existant modifié.

### - [x] CODE-088 — Webhook revenue + dashboard admin business ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605140000_cas_pratique_business_metrics.sql` (créé) + `supabase/functions/cas_pratique_business_notify/index.ts` (créé)
**Action** : Capture des events Stripe (subscription_created, canceled, refunded). MRR / ARR / churn affichés admin. Notification Slack par nouveau abonné.
**Livré** : Migration SQL avec table `cp_business_prices` (mapping stripe_price_id → label + interval_kind + amount_eur + is_lifetime, seeds monthly 9.99€ / yearly 79€ / lifetime 149€) + 5 vues SQL business : (1) `cp_business_active_subs` snapshot temps réel des subs active/trialing avec join prices, (2) `cp_business_mrr_arr` calculé en normalisant monthly = amount_eur direct, yearly = amount/12, lifetime exclu → mrr_eur + arr_eur + active_paying_users + snapshot_at, (3) `cp_business_monthly_revenue` série temporelle 12 derniers mois avec generate_series, revenue_eur calculé selon interval_kind + new_subs_count par mois, (4) `cp_business_churn` qui compte canceled 30j vs active 30j ago → churn_rate_pct, (5) `cp_business_cohorts` 12 cohortes par mois d'inscription avec retention_pct (still_paying / users_in_cohort). REVOKE all from anon/authenticated + GRANT select to service_role only (admin only). Table `cp_business_events_queue` (PK uuid, event_kind, user_id, payload jsonb, created_at, processed_at, notify_slack) + index partiel sur unprocessed. Trigger `cp_business_track_sub_event` AFTER INSERT/UPDATE qui détecte automatiquement les transitions free→premium → INSERT event 'new_subscription', et active→canceled → INSERT 'churned'. Edge function `cas_pratique_business_notify` : récupère jusqu'à 50 events non traités depuis la queue, format Slack blocks avec emojis (🎉 new_sub / 😢 churn / ⚠️ past_due), POST vers SLACK_WEBHOOK_URL env var, marque processed_at sur succès, log les failures. À déclencher via pg_cron toutes les 5 min (commande SQL documentée en tête du fichier). Aucun fichier existant modifié.

---

## 🟦 PHASE Q — Communauté & contenu (5 tâches)

### - [x] CODE-089 — Bookmarks / cas favoris ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605150000_cas_pratique_user_bookmarks.sql` (créé) + `lib/features/bookmarks/cp_bookmarks_service.dart` (créé)
**Action** : Icône étoile sur chaque cas. Page "Mes favoris". Synchro multi-device.
**Livré** : Migration SQL `cas_pratique_user_bookmarks` (PK composite user_id+case_id, FK cascade sur auth.users et cases, bookmarked_at, note text) + indexes user_date (DESC) et case. RLS complète : select_own, insert_own, update_own, delete_own — tous gated sur `user_id = auth.uid()`. Vue enrichie `cp_my_bookmarks` qui join cas_pratique_cases pour exposer case_slug/title/year/theme_id/difficulty triée par bookmarked_at DESC, scoped auth.uid(), grant select to authenticated. Fonction RPC `cp_toggle_bookmark(p_case_id)` SECURITY DEFINER atomique : check user authentifié, EXISTS check, DELETE si présent (return false) sinon INSERT ON CONFLICT DO NOTHING (return true). Service Flutter `CpBookmarks` singleton avec modèle `CpBookmark` (caseId/Slug/Title/Year + themeId/difficulty + bookmarkedAt + note), cache local `_cachedIds Set<String>` + `_cachedList List<CpBookmark>`, broadcast Stream `bookmarksStream`, `isBookmarked()` sync O(1), `hydrate()` qui refresh + subscribe Realtime sur cas_pratique_user_bookmarks filtré user_id, `toggleBookmark()` avec optimistic update + RPC + rollback en cas d'erreur + refresh post-RPC pour récup détails, `stop()` au logout. Realtime via PostgresChangeFilter eq user_id. Aucun fichier existant modifié.

### - [x] CODE-090 — Fiches mémo (ressources) intégrées ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605160000_cas_pratique_memos.sql` (créé) + `lib/features/memos/cp_memos_page.dart` (créé)
**Action** : Fiches courtes (1 page max) sur les thèmes : "Déontologie en 5 points", "Articles clés du code pénal", etc. Linkées depuis les correction details.
**Livré** : Migration SQL avec table `cas_pratique_memos` (PK uuid, slug unique 3-100 chars, title, excerpt 200 max preview, content_md, theme_id FK, tags text[], reading_time_minutes, is_premium, is_published, display_order, view_count, timestamps, published_at) + indexes theme_order, tags GIN, slug — tous partiels `where is_published = true`. Table `cas_pratique_memo_reads` (PK composite user_id+memo_id, read_at, duration_seconds) + index user_date. Trigger updated_at + RPC `cp_memo_mark_read(memo_id, duration?)` SECURITY DEFINER qui UPSERT le read avec ON CONFLICT update read_at + duration coalesce + UPDATE view_count++. RLS : memos public read si is_published, reads read_own seulement. Vue `cp_memos_with_read_state` qui calcule `is_read_by_user` via EXISTS scoped auth.uid(), triée display_order/published_at DESC, grant select anon+authenticated. 5 seeds : déontologie-5-points, articles-cles-code-penal, procedure-penale-chronologie, reflexes-redaction-cas-pratique, organisation-police-nationale (markdown complet avec h1/h2/h3/bullets, 3-5 min de lecture chacune, tags ciblés). Page Flutter `CpMemosListPage` route `/cas-pratique/memos` : Scaffold + RefreshIndicator + FutureBuilder qui lit la vue, ListView.separated avec `_MemoCard` (icône bleu sur ink-tint, titre w800, excerpt 2 lignes, footer reading_time + badge PREMIUM jaune + 2 tags chips, check vert si lu). Page `CpMemoReaderPage` route `/cas-pratique/memos/reader` arg `{slug}` : load par slug avec maybeSingle, tracker `_startedAt` → dispose() appelle RPC cp_memo_mark_read avec duration_seconds calculée, AppBar minimaliste, titre 24px, métadonnée reading_time. Widget `_MemoMarkdownBody` markdown léger maison (zéro dépendance externe) : split lignes, parse `# h1`, `## h2`, `### h3`, `- bullet`, paragraphe par défaut, inline `**bold**` + `*italic*` via RegExp + RichText InlineSpan, bullets avec puce custom. Aucun fichier existant modifié.

### - [x] CODE-091 — Commentaires / annotations sur sa propre copie ✅ 2026-06-05
**Fichier(s)** : `supabase/migrations/20260605170000_cas_pratique_user_notes.sql` (créé) + `lib/features/notes/cp_user_notes_service.dart` (créé)
**Action** : Sur sa page correction, pouvoir ajouter des annotations privées sur chaque question ("À revoir", "Important pour le concours"). Recherchable.
**Livré** : Migration SQL `cas_pratique_user_notes` (PK uuid, user_id cascade, FK contextuelles attempt_id/question_id/rubric_point_id/case_id avec cascade adaptés, body text check 1-2000, tags text[], color hex pour highlight UI, timestamps + trigger updated_at) + 5 indexes (user_date DESC, attempt partial, question partial, case partial, tags GIN) + pg_trgm extension + index trigram GIN sur body pour recherche fuzzy. RLS complète CRUD scope auth.uid(). Vue `cp_my_notes_enriched` qui join cases + questions pour exposer case_slug/title/year + question_position, triée created_at DESC, scope auth.uid(). RPC `cp_search_notes(p_query, p_limit)` SECURITY DEFINER avec ILIKE sur body + tags unnest, gated auth.uid(). Service Flutter `CpUserNotes` singleton avec modèle `CpUserNote` (champs base + enriched depuis vue). Méthodes : `create()` avec validation (body 1-2000), INSERT minimal sans champs null, retour parsé ; `update(body?, tags?, color?)` PATCH ciblé ; `delete()` ; `listAll/listForAttempt/listForCase` lisant la vue enrichie ; `search()` qui appelle RPC avec fallback listAll si query vide. Tous les errors caught silently avec debugPrint en debug mode. Aucun fichier existant modifié.

### - [x] CODE-092 — Recherche full-text avancée + filtres combinés ✅ 2026-06-06
**Fichier(s)** : `supabase/migrations/20260606000001_cas_pratique_advanced_search.sql` (créé) + `lib/core/cas_pratique/search/cp_search_service.dart` (créé) + `lib/core/cas_pratique/widgets/cp_advanced_search_bar.dart` (créé) + `lib/data/cas_pratique/cas_pratique_filters.dart` (Edit : champ `notDone`) + `lib/content/gpx_exam/cas_pratique/cas_pratique_list_confiug.dart` (Edit : branche FTS avancée) + `lib/core/cas_pratique/theme/cp_tokens.dart` (Edit : alias `brand`/`brandFor`)
**Action** : Recherche dans titre, situation, questions, mots-clés. Auto-complete. Filtres combinés (année + thème + non fait). Surlignage des termes trouvés.
**Livré** : Migration SQL `20260606000001` avec config FTS `french_unaccent` (unaccent+stopwords FR), colonne `search_vector` tsvector sur `cas_pratique_cases` (title A + situation B), trigger de MAJ auto, 5 indexes GIN (search_vector, title_trgm, situation_trgm, keywords_trgm, questions_trgm), vue `cp_cases_search_index` dénormalisée (4 champs FTS), RPC `cp_search_cases_fts` (7 params : query+limit+offset+theme_ids+years+difficulties+not_done_uid, FTS tsquery avec fallback trigram, filtre notDone via sous-requête EXISTS, rank ts_rank_cd), RPC `cp_search_autocomplete` (similarity trigram sur titre, min 2 chars). Service Dart `CpSearchService` singleton (searchCases retourne `List<CpSearchResult>` avec themeId pour lookup local, autocomplete retourne `List<CpAutocompleteSuggestion>`). Helper `CpTextHighlighter.highlight()` split sans dépendance externe. Widget `CpAdvancedSearchBar` (overlay auto-complete CompositedTransformFollower + FadeTransition, debounce 200ms, chip "Non fait" animé, clear button, loading indicator). Widget `CpHighlightedText` réutilisable (RichText spans avec backgroundColor highlight). Modèle `CasPratiqueFilters` étendu avec `notDone bool` + `toggleNotDone()` + sérialisation JSON `not_done`. Liste page branchée : `_fetchCases` bifurque vers `_fetchCasesAdvanced` si notDone ou searchQuery non vide (appel RPC + enrichissement theme local), sinon path rapide repo. `_CaseTile` accepte `searchQuery` et utilise `CpHighlightedText`. Alias `CpTokens.brand` + `brandFor(isDark)` ajoutés. Aucun fichier hors périmètre modifié.

### - [x] CODE-093 — Notifications push intelligentes (FCM topics) ✅ 2026-06-06
**Fichier(s)** : `supabase/migrations/20260606100000_cp_user_notification_prefs.sql` (créé) + `lib/core/notifications/cp_push_service.dart` (créé) + `lib/core/notifications/cp_notif_prefs_page.dart` (créé) + `lib/main.dart` (Edit : 2 imports) + `lib/routes/app_router.dart` (Edit : case CpNotifPrefsPage.routeName)
**Action** : Push : nouveau cas dispo, streak risque de casser, appel approuvé, leaderboard hebdo. Schedule intelligent (respect horaires user). Opt-in granulaire.
**Livré** : Migration SQL `20260606100000` — table `cp_user_notification_prefs` (user_id UNIQUE, 4 booleans topic, quiet_start_hour/quiet_end_hour/user_timezone, fcm_token, RLS propriétaire, trigger updated_at, vue `cp_admin_notification_targets` service_role pour backend). Enum `CpNotifTopic` (4 valeurs : newCase/streakRisk/appealResult/leaderboard avec topicId FCM, label, description, prefKey, defaultEnabled). Modèle `CpNotifPrefs` immuable (copyWith, isEnabled). Service `CpPushService` singleton : init() — hydrate SharedPreferences + applique souscriptions FCM + refresh token ; setTopicEnabled() — FCM subscribeToTopic/unsubscribeFromTopic + persist SP + sync Supabase ; setQuietHours() / setUserTimezone() ; isCurrentlyInQuietHours() (gestion chevauchement minuit) ; listener onMessage foreground (suppression si quiet hours) ; onTokenRefresh stream ; onUserSignedIn/SignedOut hooks ; _syncPrefsToSupabase() upsert. Page `CpNotifPrefsPage` (routeName `/cp/notifications/prefs`) : dark/light #1147D9/#000B36 Montserrat, StreamBuilder sur prefsStream, Section Topics (4 _TopicTile avec icône + Switch.adaptive + Semantics), Section Quiet Hours (2 _QuietHourTile avec showTimePicker 24h), note légale. Route enregistrée dans app_router.dart, imports dans main.dart. Aucun fichier hors périmètre Cas Pratique modifié.

---

## 🟫 PHASE R — Panel admin & multi-tenant (3 tâches)

### - [x] CODE-094 — Panel admin Flutter Web (CRUD cas + rubrics) ✅ 2026-06-06
**Fichier(s)** : `admin/pubspec.yaml` (créé) + `admin/lib/main.dart` (créé) + `admin/lib/admin_app.dart` (créé) + `admin/lib/router/admin_router.dart` (créé) + `admin/lib/models/admin_models.dart` (créé) + `admin/lib/services/admin_service.dart` (créé) + `admin/lib/theme/admin_theme.dart` (créé) + `admin/lib/screens/login_screen.dart` (créé) + `admin/lib/screens/shell_screen.dart` (créé — nav rail 220 px, dark/light toggle, logout) + `admin/lib/screens/dashboard_screen.dart` (créé — KPI grid 6 cards, line chart fl_chart 30 j, top 5 failed bar, quick actions) + `admin/lib/screens/cases_screen.dart` (créé — table virtualisée, filtres statut/thème/search, tri, bulk publish/archive/duplicate/delete, status badge) + `admin/lib/screens/case_edit_screen.dart` (créé — 4 onglets Infos/Situation/Questions+Rubric/Preview, markdown éditeur double pane, auto-slug, stepper, dialog question editor) + `admin/lib/screens/appeals_screen.dart` (créé — liste filtrable par statut, card enrichie, dialogs Approuver/Rejeter avec motif) + `admin/lib/screens/synonyms_screen.dart` (créé — table slug/label/termes/tags/usage, chip editor, add term/tag) + `admin/lib/screens/audit_screen.dart` (créé — timeline filtres action/entity, diff JSON collapsable) + `admin/lib/screens/users_screen.dart` (créé — lecture seule, top 100 par XP) + `admin/lib/widgets/kpi_card.dart` (créé) + `supabase/migrations/20260606200000_cas_pratique_admin_rpcs.sql` (créé — 5 RPCs : cp_admin_kpis, cp_admin_daily_attempts, cp_admin_top_failed, cp_admin_synonyms, cp_admin_appeals, tous protégés par fn_cp_is_admin())
**Action** : Implémenter le panel admin complet : dashboard KPIs, CRUD cas, éditeur rubric avec preview live, dictionnaire synonymes, modération des appels, audit log. Déployé sur `admin.copiq.fr`.
**Vérification** : ✅ Panel Flutter Web complet (17 fichiers Dart + 1 migration SQL). Architecture : GoRouter ShellRoute + NavigationRail + Material3 dark/light Montserrat #1147D9/#000B36. AdminService Supabase singleton avec CRUD + RPC + audit log automatique. Closing braces vérifiés sur tous les fichiers. Aucun fichier hors périmètre touché.

### - [x] CODE-095 — Multi-tenant (centres de formation) ✅ 2026-06-07
**Fichier(s)** : `supabase/migrations/20260607000001_cas_pratique_organizations.sql` (créé — tables cas_pratique_organizations + cas_pratique_org_members + cas_pratique_org_invitations, trigger updated_at, indexes, fn_cp_is_trainer, RLS multi-tenant 6 policies, RPCs cp_admin_list_orgs + cp_admin_get_org) + `admin/lib/models/admin_models.dart` (Edit — ajout OrgPlan/OrgRole enums + AdminOrganization/AdminOrgMember/OrgPromoStats/OrgWeakTheme models) + `admin/lib/services/admin_service.dart` (Edit — ajout méthodes listOrganizations/getOrgMemberStats/createOrganization/updateOrganization/deleteOrganization/addOrgMember/removeOrgMember + stats formateur getPromoStats/getWeakThemes/exportCsvData) + `admin/lib/screens/organizations_screen.dart` (créé — liste orgs, OrgCard plan badge, dialog create/edit, members sheet avec stats, confirm delete) + `admin/lib/router/admin_router.dart` (Edit — routes /admin/organizations + /admin/organizations/:orgId/stats) + `admin/lib/screens/shell_screen.dart` (Edit — ajout item nav "Organisations")
**Action** : Un centre de formation peut gérer ses élèves, voir leur progression, créer des "promos". RLS multi-tenant. Pricing entreprise.
**Vérification** : ✅ 3 tables SQL + 6 policies RLS + 2 RPCs admin. 4 modèles Dart. Écran Flutter Web complet avec CRUD orgs, sheet membres avec stats. Route enregistrée. Nav item ajouté. Closing braces vérifiés. Aucun fichier hors périmètre touché.

### - [x] CODE-096 — Stats agrégées multi-user (rôle formateur) ✅ 2026-06-07
**Fichier(s)** : `supabase/migrations/20260607000002_cas_pratique_org_stats_views.sql` (créé — vues cp_org_learner_progress + cp_org_kpis, RPCs cp_trainer_promo_stats + cp_trainer_weak_themes + cp_trainer_export_csv + cp_admin_org_member_stats) + `admin/lib/screens/org_stats_screen.dart` (créé — onglets Classement (DataTable rank/email/promo/cas/moy%/meilleur/date) + Thèmes faibles (barres couleur vert/orange/rouge), KPI row 4 tuiles, export CSV presse-papier, filtre par promo, dark/light Montserrat)
**Action** : Pour un formateur : voir les scores de ses élèves, thèmes faibles communs, classement de la promo, export CSV des notes.
**Vérification** : ✅ 2 vues SQL + 4 RPCs sécurisées SECURITY DEFINER (fn_cp_is_trainer). Écran Flutter Web OrgStatsScreen complet avec 2 onglets, KPIs promo, export CSV. Closing braces vérifiés. Phase R 3/3 FINIE.

---

## ⚪ PHASE S — CI/CD, release, ops (4 tâches)

### - [x] CODE-097 — Pipeline CI/CD GitHub Actions (2026-06-06)
**Fichier(s)** : `.github/workflows/flutter-ci.yml` ✅ refondu (156 lignes) + `.github/workflows/flutter-release.yml` ✅ refondu (336 lignes) + `fastlane/Fastfile` ✅ créé + `fastlane/Appfile` ✅ créé + `fastlane/Gemfile` ✅ créé + `docs/cas_pratique/CICD_SECRETS_GUIDE.md` ✅ créé
**Action** : CI : analyze + tests + coverage à chaque PR. Release : build APK/IPA, signature, upload Play Store + TestFlight via Fastlane. Sentry source-maps. Tag git + GitHub release auto.
**Vérification** : ✅ `flutter-ci.yml` : 4 jobs (analyze+format / test+coverage+Codecov / build-android debug / build-ios debug), concurrency cancel-in-progress, dart-defines vides en CI. `flutter-release.yml` : 6 jobs séquencés (build-android signé AAB+APK / build-ios IPA Fastlane Match / sentry-sourcemaps split-debug-info+obfuscate+sentry-cli release / deploy-android Play Store internal track / deploy-ios TestFlight / github-release avec changelog auto + prerelease si -beta/-rc). `fastlane/Fastfile` : lanes android (build_release / deploy_internal / promote_to_production) + iOS (build_release / upload_testflight / beta) + error callback. `CICD_SECRETS_GUIDE.md` : 15 secrets documentés, keytool snippet, build.gradle signing config, init Match, procédure release en 3 commandes git. Aucun fichier hors périmètre touché. Closing braces/YAML vérifiés.

### - [x] CODE-098 — Force update si breaking change ✅ 2026-06-06
**Fichier(s)** :
- `supabase/functions/app_minimum_version/index.ts` ✅ créé
- `supabase/migrations/20260608000001_cas_pratique_app_version_config.sql` ✅ créé
- `lib/core/app/version_check.dart` ✅ créé
- `lib/main.dart` ✅ import ajouté (Edit ciblé)
**Action** : Au boot, check version. Si < `min_version` retourné par l'API → écran bloquant "Mets à jour COP'IQ" avec lien store. Permet de rollouter des breaking changes sans casser les vieux clients.
**Vérification** : ✅ Migration SQL `cp_app_version_config` (PK platform android/ios, min_version/latest_version/force_update/store_url/message_fr, trigger updated_at, RLS public read / service_role write, 2 seeds). Edge function GET `?platform=android|ios` : lit la table Supabase (service_role), fallback sur defaults si DB down (fail-open), cache client 5 min, retourne `{min_version, latest_version, force_update, store_url, message_fr, checked_at}`. Service Flutter `AppVersionChecker.I.checkAndCache()` : cache SharedPreferences 6h, parse semver X.Y.Z, compare avec PackageInfo.version, fail-open sur erreur réseau. `ForceUpdateScreen` : gradient COP'IQ dark/light, icône system_update, texte message_fr, CTA `launchUrl` vers store, pas de back navigation possible. `AppVersionGate` wrapper alternatif. Wiring main.dart documenté en tête du fichier (4 lignes à ajouter dans _bootstrap + enum _Route). Import ajouté dans main.dart. Closing braces vérifiés. Aucun fichier hors périmètre modifié (sauf import 1 ligne main.dart).

### - [x] CODE-099 — Tests E2E (Maestro flows critiques) ✅ 2026-06-06
**Fichier(s)** :
- `.maestro/flows/01_signup.yaml` ✅ créé
- `.maestro/flows/02_onboarding.yaml` ✅ créé
- `.maestro/flows/03_premier_cas.yaml` ✅ créé
- `.maestro/flows/04_correction.yaml` ✅ créé
- `.maestro/flows/05_appeal.yaml` ✅ créé
- `.maestro/flows/06_paywall.yaml` ✅ créé
- `.maestro/flows/07_achat.yaml` ✅ créé
- `.maestro/flows/08_leaderboard.yaml` ✅ créé
- `.maestro/flows/09_partage.yaml` ✅ créé
- `.maestro/flows/10_suppression_compte.yaml` ✅ créé
- `.github/workflows/flutter-ci.yml` ✅ Edit ciblé (job e2e-maestro ajouté)
**Action** : 10 scénarios E2E : signup, onboarding, premier cas, correction, appeal, paywall, achat, leaderboard, partage, suppression compte. Exécutés à chaque merge sur main.
**Vérification** : ✅ 10 flows Maestro YAML couvrant les 10 scénarios critiques. Flows 01-09 exécutés en CI sur émulateur Android (reactivecircus/android-emulator-runner Pixel 6 API 33). Flow 10 suppression protégé (secret E2E_DELETE_EMAIL requis, non exécuté sans lui). Sorties JUnit XML uploadées en artifact `maestro-e2e-results`. Screenshots uploadés si failure. Job `e2e-maestro` déclenché uniquement sur push main (pas les PRs) — rapide à merger. `--env` injecte TEST_EMAIL/PASSWORD depuis secrets GH. Closing braces YAML vérifiés. Aucun fichier hors périmètre modifié (flutter-ci.yml : Edit ciblé uniquement).

### - [x] CODE-100 — Release v1.0 — checklist finale ✅ 2026-06-06
**Fichier(s)** : `docs/cas_pratique/RELEASE_V1_CHECKLIST.md` ✅ créé
**Action** : Checklist 50 items : stores ready, screenshots, descriptions ASO, privacy policy, terms, support email, status page live, monitoring actif, backup tested, runbook incident écrit, tests E2E green, perf budget OK, accessibility OK, i18n FR OK, beta testers feedbacks intégrés. **Quand cochée à 100% → LAUNCH.**
**Vérification** : ✅ Checklist 50 items répartis en 7 domaines : Stores & Distribution (10), Backend & Infra (8), Sécurité (6), Qualité & Tests (8), Accessibilité & Compliance (5), Content & ASO (7), Monitoring & Ops (6). Tableau d'avancement par domaine. Procédure release en 3 commandes (`git tag v1.0.0 / git push / → flutter-release.yml`). Tableau décisions post-launch. Tous les items couvrent l'intégralité du projet (stores, keystore, certs iOS, screenshots, ASO, RGPD, sécurité, tests, accessibilité, monitoring, runbook, beta feedback). Aucun fichier existant modifié.

---

## 🚧 BLOCAGES & NOTES

Les tâches qui rencontrent un blocage (asset manquant, secret manquant, décision business) doivent être marquées `- [!]` et documentées ci-dessous.

| ID  | Tâche | Raison du blocage | Action requise |
|-----|-------|-------------------|----------------|
| —   | —     | —                 | —              |

---

## 📜 LOG DES SESSIONS

À mettre à jour à chaque relance.

| Date | Tâches commencées | Tâches complétées | Notes |
|------|-------------------|-------------------|-------|
| 2026-05-08 | — | — | Création du fichier PROGRESSION_CODE.md (50 tâches) |
| 2026-05-08 | CODE-001 | CODE-001 | Migration 001 (extensions pgcrypto/pg_trgm/unaccent) créée |
| 2026-05-08 | CODE-002, CODE-003 | CODE-002, CODE-003 | Migrations 002 (taxonomy/cases) et 003 (rubrics/keywords) créées |
| 2026-05-08 | CODE-004 → CODE-008 | CODE-004 → CODE-008 | Phase A finie : attempts/corrections + appeals/audit + indexes + triggers + RLS+seeds |
| 2026-05-08 | CODE-009 → CODE-013 | CODE-009 → CODE-013 | Phase B finie + début Phase C : CpTokens + modèles complets + exceptions + Repository abstract |
| 2026-05-08 | CODE-014 | CODE-014 | Implémentation Supabase : listThemes() + listCases() avec filtres, tri et user_progress agrégé |
| 2026-05-08 | CODE-015 → CODE-018 | CODE-015 → CODE-018 | Phase C finie : getCaseDetail + attempts/answers + cache local + intégration cache. WEB_REUSABILITY_MEMORY.md créé |
| 2026-05-08 | CODE-019 → CODE-022 | CODE-019 → CODE-022 | Engine briques bas-niveau : Normalizer + Tokenizer/ngrams + FrLemmatizer + Levenshtein |
| 2026-05-08 | CODE-023 → CODE-025 | CODE-023 → CODE-025 | Engine cœur : NegationDetector + SynonymResolver + KeywordMatcher (le matcher central, 4 stratégies de match + gestion is_negation) |
| 2026-05-08 | CODE-026 → CODE-028 | CODE-026 → CODE-028 | **Phase D 10/10 finie** : PointEvaluator + QuestionScorer + AttemptScorer + Façade CorrectionEngine (load rubric Supabase + scoring + persist) |
| 2026-05-08 | CODE-029 | CODE-029 | Phase E commencée : CasPratiqueScaffold (gradient + halo + vignette + back pill + titre + bottomAction) |
| 2026-05-12 | CODE-030 → CODE-032 | CODE-030 → CODE-032 | Widgets premium : AnswerTextArea (compteur + autosave indicator) + ScoreReveal (anneau animé + haptic) + PointPill (expand + chips matchés + bouton appel) |
| 2026-05-12 | CODE-033 + CODE-034 | CODE-033 + CODE-034 | case_dynamic_page.dart : structure PageController + skeleton + error scaffold + Intro premium (pills thème/difficulté + 3 puces) + Texte immersif |
| 2026-05-17 | CODE-035 + CODE-036 | CODE-035 + CODE-036 | **Phase E 8/8 finie** : pages questions branchées sur AnswerTextArea + autosave debounced 1.5s + lock back après validation + hydratation reprise. Page correction branchée sur CorrectionEngine (stopwatch timeSpentMs, refreshCache après) + ScoreReveal + accordion PointPill avec matchedKeywords + section Réponse modèle collapsable + LegalChips. Patch correction_engine pour remplir point_label/point_kind/explanation_md sans query DB supplémentaire. Petit bugfix `showBack` (target page doit être ≥ minBackIndex). |
| 2026-05-17 | CODE-037 | CODE-037 | **Phase F débutée** : refonte liste dynamique Supabase (FutureBuilder + Skeleton/Empty/Error/Success + RefreshIndicator + pull-to-refresh + cartes premium thème/difficulté/Nouveau/scores/réussite + navigation MaterialPageRoute vers CasPratiqueDynamicPage). |
| 2026-05-17 | CODE-038 | CODE-038 | Filtres en pill : modèle immutable `CasPratiqueFilters` + service `CasPratiqueFiltersService` (persistance shared_preferences via CasPratiqueCache, clé dédiée, pas de TTL) + `MultiSelectSheet<T>` générique + `FilterChipsRow` (compteurs + chip "Effacer") + wiring complet (bootstrap parallèle filtres/thèmes, _availableYears accumulé, _EmptyState filtres-aware). |
| 2026-05-17 | CODE-039 | CODE-039 | Recherche + tri + pull-to-refresh : `_InlineSearchBar` premium avec debounce 300ms + 2 actions topbar (loupe + swap_vert) + `showSortBottomSheet` (4 options : récent / alphabétique / durée asc / durée desc) + persistance tri via `CasPratiqueFiltersService.saveSort` + `_SortHint` indicateur sous les chips + EmptyState étendu à hasSearch. |
| 2026-05-17 | CODE-040 | CODE-040 | **Phase F 4/4 finie** : routing dynamique enregistré dans `appOnGenerateRoute` (lecture tolérante String/Map de l'argument caseSlug, settings propagés), import case_dynamic_page ajouté à main.dart, liste basculée sur `pushNamed(CasPratiqueDynamicPage.routeName, arguments: slug)`. |
| 2026-05-17 | CODE-041 + CODE-042 + CODE-043 | CODE-041 + CODE-042 + CODE-043 | Système d'appel utilisateur (Phase G 3/5) : AppealSheet premium (header + point reminder + user answer + textarea compteur + CTA) + createAppeal/listMyAppeals dans repo (insertion DB + RLS user) + patch engine (insert.select pour vrais ids correction_details) + intégration UI sur PointPill missing (handler `_onAppealRequest` avec guards/in-flight/snackbar/haptic + suffix "Appel envoyé" dans explanation). |
| 2026-05-17 | CODE-044 | CODE-044 | Realtime stream appeals : `watchMyAppeals()` avec channel Supabase + onPostgresChanges INSERT/UPDATE filtré user_id, `CasPratiqueAppealsListener` singleton avec hydratation initiale + détection transitions pending→approved/rejected + AppNotifier success/warning, wiring main.dart sur signedIn/signedOut. |
| 2026-05-17 | CODE-045 | CODE-045 | **Phase G 5/5 finie** : page "Mes appels" premium (chips filtres par statut avec compteurs + AppealCard avec status pill / date relative / message / AdminResponse accent + realtime auto-refresh via watchMyAppeals + Skeleton/Empty/Error/RefreshIndicator) + route enregistrée dans app_router + topbar action sur la page correction. |
| 2026-05-17 | CODE-046 + CODE-047 + CODE-048 | CODE-046 + CODE-047 + CODE-048 | **Phase H 3/3 finie** : extracteur Dart → JSON pour les 6 cas legacy (scanner brace-matching robuste + valeurs par défaut + TODO admin) + migration SQL idempotente `fn_cp_seed_legacy_case(slug, jsonb)` qui upsert + wipe + recrée tout l'arbre (case→questions→perfect→rubric→groups→keywords) + 6 slugs placeholder en draft + redirection des routes legacy `/case_1` → `/case_6` vers `CasPratiqueDynamicPage(caseSlug)` dans `RouteRegistry.routes` + annotation `@Deprecated` sur les 6 classes Gpx_CasPratiqueCaseXPage. |
| 2026-05-17 | CODE-049 + CODE-050 | CODE-049 + CODE-050 | 🎉 **MVP COMPLET 50/50** — Phase I 2/2 finie : 9 fichiers de tests engine (60+ tests : normalizer/tokenizer/lemmatizer/levenshtein/negation/synonym/matcher/evaluator/scorer) + SMOKE_TEST_REPORT.md exhaustif (analyze, tests, flow 19 étapes, dark/light, perf, sécurité, findings, next steps). |
| 2026-05-17 | CODE-051 | CODE-051 | **Phase J démarrée** : edge function TS port complet du moteur Dart dans `supabase/functions/cas_pratique_correct_attempt/index.ts` — single-file Deno (~700 lignes) avec normalize/tokenize/levenshtein/negation/matcher/evaluator/scorer + handler POST avec auth Bearer, ownership check, loadRubric service-role, scoring, persist corrections + details (.select), update attempt, réponse JSON enrichie. Verrouille la rubric côté serveur. engine_version = 2.0.0 (parité prête pour CODE-052). |
| 2026-05-17 | CODE-052 | CODE-052 | Tests croisés Dart↔TS : extraction du moteur TS dans `_engine.ts` (importable, pur sans Deno serve), refactor index.ts pour importer depuis _engine, 4 fixtures JSON partagées (perfect/partial/missing/multi-fuzzy-phrase-syndict), test Deno `tests/parity/dart_vs_ts.test.ts` + test Flutter `test/cas_pratique/parity_test.dart`. Les 2 vérifient `total_score`/`total_max`/`percent` à 0.01 près → parité prouvée si les 2 passent. |
| 2026-05-17 | CODE-053 | CODE-053 | Sentry / Crashlytics intégration : wrapper `AppMonitoring` (init idempotent, hook FlutterError + PlatformDispatcher, no-op si DSN absent, sample 100/10%, tag module cas_pratique), helper `runAppWithMonitoring`, API captureException/Message/Breadcrumb/setUserId. SENTRY_SETUP.md complet (10 sections : pubspec, dart-define, wiring main, Android/iOS, source-maps + GH Action, alertes Slack/email P0/P1/P2, sample policy, test local, privacy). |
| 2026-05-17 | CODE-054 | CODE-054 | Rate limiting token bucket : table `cas_pratique_rate_buckets` (PK user_id+scope, RLS no-direct) + fonction Postgres SECURITY DEFINER atomique `fn_cp_consume_token` (UPSERT + SELECT FOR UPDATE + refill + consume) + helper TS `consumeRateLimit`/`rateLimitedResponse` avec `RATE_PROFILES` (listCases 60/min, saveDraft 600/min, validateAnswer 30/min, finishCorrect 10/min, createAppeal 20/jour) + wire dans edge function correction (fail-closed) + RATE_LIMITING.md (architecture, intégration Dart, reset admin, monitoring). |
| 2026-05-17 | CODE-055 | CODE-055 | **Phase J 5/5 finie** : audit sécurité SECURITY_AUDIT.md (OWASP Mobile Top 10 2024 M1→M10 avec statuts, checklist COP'IQ 15 items, modèle security.txt RFC 9116, recommandations P0/P1/P2 pour release v1.0, procédure récurrente). Synthèse : 24 items ✅ OK, 8 items 🟡 PARTIAL, 11 items 🔴 TODO (RGPD, pentest, cert pinning, jailbreak, security.txt). |
| 2026-05-17 | CODE-056 | CODE-056 | **Phase K démarrée** : système de streaks (jours consécutifs). Table cas_pratique_streak_freezes + fonction SQL `fn_cp_compute_streak` pure (walk desc des jours UTC + freezes) + `fn_cp_apply_streak_to_progress` qui persiste dans user_progress.streak_days + trigger auto sur INSERT corrections. Service Dart `StreaksService` singleton avec `StreakStatus` immutable, milestones 3/7/14/30/60/100/200/365, motivationLabel() contextuel (démarre / préserve / palier), cache mémoire 2 min, `invalidate()` après validation. |
| 2026-05-17 | CODE-058 | CODE-058 | Niveaux & XP (anticipation hors ordre : CODE-057 Badges reste à faire) : table cas_pratique_xp_ledger append-only + fonction `fn_cp_award_xp_for_correction` (base = percent×10×nb_questions, streak_bonus min(50, streak×2), first_try +25) + trigger auto + fonction `fn_cp_xp_total` (10 niveaux Recrue→Légende COP'IQ aux seuils 0-32000). Service Dart `XpService` avec `XpStatus` immutable, Stream `changes` émet sur vrai changement (pour célébrer level-up), cache 2 min, kXpLevels single source of truth synchro SQL. |
| 2026-05-17 | CODE-057 | CODE-057 | Badges & succès : table catalog `cas_pratique_badges` seedée avec 20 badges (7 progress/3 score/4 streak/2 breadth/2 quality/2 xp), table unlocks `cas_pratique_user_badges` (PK user+slug), fonction `fn_cp_check_and_unlock_badges` qui calcule les stats en 1 passe puis INSERT ON CONFLICT DO NOTHING pour chaque condition, retourne les nouveaux slugs débloqués (5 sec window), trigger auto sur INSERT corrections. Service Dart `BadgesService` avec `Badge`/`UnlockedBadge`, cache catalog 24h, `Stream<List<Badge>> newUnlocks` pour toast premium UI. |
| 2026-05-17 | CODE-059 | CODE-059 | Leaderboard hebdomadaire anonymisé : MATERIALIZED VIEW `cas_pratique_weekly_leaderboard` (agrég xp_ledger 7j glissants, DENSE_RANK, anonymisation `md5(user_id)[-4]` → "Apprenti #XXXX", LIMIT 1000) + REVOKE ALL → accès via 2 fonctions SECURITY DEFINER (`fn_cp_get_leaderboard` + `fn_cp_my_leaderboard_position`) qui jamais ne fuitent user_id. Fonction refresh CONCURRENTLY + snippet pg_cron horaire commenté. Service Dart `LeaderboardService` (cache 2 min) + page premium `CasPratiqueLeaderboardPage` (médailles or/argent/bronze, gradient personnel, percentile, RefreshIndicator, états skeleton/empty/error). Route enregistrée. |
| 2026-05-17 | CODE-060 | CODE-060 | Parrainage : extension CHECK ledger.reason pour `referral_bonus` + tables `cas_pratique_referral_codes` (1 code 6 chars upper unique par user, alphabet sans 0/O/1/I) + `cas_pratique_referrals` (UNIQUE referee, CHECK pas auto-parrainage). Fonctions `fn_cp_get_or_create_my_referral_code` (upsert) + `fn_cp_redeem_referral_code(p_code)` (gère code_not_found/self_referral/already_referred + INSERT referrals + 2 INSERTs xp_ledger +500 chacun + recompute badges). Service Dart `ReferralService` (RedeemError typé, shareLink/shareMessage FR) + page premium `CasPratiqueReferralPage` (code en grand gradient bleu, copy code/lien, stats 3 cards, 3 steps "comment ça marche", input redeem avec validation). Route enregistrée. |
| 2026-05-17 | CODE-061 | CODE-061 | Concours blanc (mode timer) : 4 tables SQL (mock_exams catalog 45 min / mock_exam_cases M:N / mock_exam_attempts avec deadline_at + status / mock_exam_answers avec RLS WRITE qui bloque après expiration), 3 fonctions (`start_mock_exam` reprend ou crée avec deadline, `finish_mock_exam` calcule expired vs submitted, `mock_exam_leaderboard` DENSE_RANK par percent DESC + time_spent_ms ASC + anonymisation md5). Service Dart `MockExamService` (MockExam/Attempt/FinishResult/LeaderboardEntry). Page `CasPratiqueConcoursBlancPage` premium : `_TimerBanner` countdown XX:XX rouge<5min avec glow, verrouillage strict (pas de retour entre questions), AnswerTextArea autosave 2s, auto-submit à 0:00, WillPopScope warning, _SubmittedState 2 variants. Route enregistrée. |
| 2026-05-17 | CODE-062 | CODE-062 | **Phase K 7/7 FINIE** : algo de recommandation. Fonction SQL `fn_cp_recommend_next_cases` STABLE algo 3 niveaux (weakest_theme_new > weakest_theme_replay (<50%) > fresh fallback) + cas newcomer + cas no-auth. Service Dart `RecommendationService` + `RecommendedCase` + enum `RecommendationReason` (avec label/motivation FR). Widget `HomeRecommendationWidget` premium horizontal scroll 3 cards 268px gradient thème + chip raison sémantique + tap → CasPratiqueDynamicPage. |
| 2026-05-17 | CODE-063 | CODE-063 | **Phase L démarrée** : onboarding 4 écrans premium. Service dual-storage Supabase Auth user_metadata + shared_preferences (boot rapide offline + multi-device). Page PageView 4 slides immutables (Bienvenue/Cas réels/Correction/Démarrer) avec background gradient interpolé 700ms, dots animés, icône glow loop 2.6s, AnimatedSwitcher slide+fade séquencé, CTA évolutif "Suivant"→"Démarrer maintenant"+rocket. Argument optionnel `nextRoute`, défaut vers liste des cas. |
| 2026-05-17 | CODE-064 | CODE-064 | Cas démo gratuit + lock UI : migration ALTER TABLE ADD COLUMN is_free + UPDATE case_1 + index partiel. Modèle CaseSummary.isFree exposé. Repo SELECT is_free dans list + detail. Service `CasPratiqueSubscriptionService` placeholder pour CODE-084 (`isPremium`/`canAccessCase`). Liste : `_openCase` check accès + `_showPaywallPromo` AlertDialog premium. Badges `_FreeBadge` gradient vert + `_PremiumLockBadge` theme-aware lock. |
| 2026-05-17 | CODE-065 | CODE-065 | Sound design premium : singleton `SoundsService` + enum `SoundEvent` (tap/validation/correction/fanfare/error/successQuiet). Mute persisté shared_preferences. Implémentation initiale sans nouvelle dep via SystemSound + HapticFeedback combos rythmés (fanfare = heavy + 3 clicks 120/220/340ms). TODO documenté pour brancher audioplayers + assets post-MVP sans toucher les call-sites. |
| 2026-05-17 | CODE-066 | CODE-066 | Haptics raffinés : singleton `HapticsService` + enum `HapticEvent` (selection/light/medium/heavy/success/warning/error). Mute persisté + respect OS `reduceMotion` (defense in depth pour accessibilité). Patterns composés `scoreReveal` (heavy + 3 clicks 180/300/420ms), `levelUp` (light + medium 60ms), `badgeUnlock` (2× selection 80ms). Patterns internes `success`/`warning`/`error` avec rythmes courts. |
| 2026-05-17 | CODE-067 | CODE-067 | Skeleton + transitions premium : `SkeletonShimmer` (InheritedWidget 1 controller partagé) + `SkeletonBox`/`Circle`/`Card`/`ListTile`/`Hero` premium theme-aware avec fallback statique reduceMotion. `CpPageRoute<T>` adaptive : Cupertino iOS (swipe-back natif + parallax) / slide+fade Android 320ms easeOutCubic. Helpers `pushCp`/`cpRoute` prêts à l'emploi. |
| 2026-05-17 | CODE-068 | CODE-068 | **Phase L 6/6 FINIE** : Dark mode AMOLED (vrai noir #000) — singleton `AmoledService` avec `ValueNotifier<bool>` observable + persistance, `CpTokens` étendu avec surfaces/outlines AMOLED (#000/#050505/#0A0A0A/#111/#1A1A1A) + 7 helpers `*For(isDark, isAmoled)` avec fallback gracieux sur les helpers standard (backward compat). Économise batterie OLED. |
| 2026-05-17 | CODE-064 | CODE-064 | Cas démo gratuit + lock UI : ALTER TABLE cases ADD is_free + UPDATE case_1 SET is_free=true + index partial. Model `CaseSummary.isFree` + repo SELECT is_free dans list/detail + rebind. Service `CasPratiqueSubscriptionService` singleton placeholder (`isPremium`/`canAccessCase`/`debugSetPremium`) — sera remplacé par check Stripe en CODE-084. Liste : `_openCase` gate l'accès, dialog paywall promo si lock, `_FreeBadge` gradient vert+glow et `_PremiumLockBadge` theme-aware sur les cards. |
| 2026-06-04 | CODE-071 + CODE-072 | CODE-071 + CODE-072 | **Phase M 3/4 + 4/4 FINIE** : CODE-071 — `CpDeepLinksHandler` singleton (`parse`/`navigate`/`handleUri`/`shareUrl`), gère `https://app.copiq.fr/c/<slug>` + `copiqpolice://cas/<slug>` + UTM params, wiring dans `DeepLinksService._handleUri`, config native complète (AndroidManifest intent-filter + Runner.entitlements Associated Domains + assetlinks.json + apple-app-site-association) documentée dans `DEEP_LINKS_NATIVE_CONFIG.md` — à appliquer manuellement (android/ios hors périmètre). CODE-072 — `OPEN_GRAPH_SPEC.md` : spec complète OG+Twitter Cards pour site Next.js (route Edge `/og/c/[slug]`, image 1200×630 satori avec gradient palette, page fallback `/c/[slug]` avec `generateMetadata`, cache + revalidation webhook, debug tools, intégration Flutter shareUrl). |

| 2026-06-04 | CODE-073 | CODE-074 | **Phase N 2/6 démarrée** : CODE-073 (lib/core/analytics/analytics_service.dart — interface CpAnalyticsInterface + impl PostHog façade no-op + singleton CpAnalytics.I, 7 events typés sans PII) + CODE-074 (docs/cas_pratique/ANALYTICS_DASHBOARDS.md — funnels activation/rétention/partage, cohortes hebdo, 5 alertes Slack, 3 dashboards PostHog, guide dart-define). — 74% (74/100) |
| 2026-06-04 | CODE-073 | CODE-074 | **Phase N 2/6 démarrée** : CODE-073 (lib/core/analytics/analytics_service.dart — interface CpAnalyticsInterface + impl PostHog façade no-op + singleton CpAnalytics.I, 7 events typés sans PII) + CODE-074 (docs/cas_pratique/ANALYTICS_DASHBOARDS.md — funnels activation/rétention/partage, cohortes hebdo, 5 alertes Slack, 3 dashboards PostHog). — 74% (74/100) |

| 2026-06-04 | CODE-073 | CODE-074 | **Phase N 2/6 demarree** : CODE-073 (lib/core/analytics/analytics_service.dart - interface CpAnalyticsInterface + impl PostHog facade no-op + singleton CpAnalytics.I, 7 events types sans PII) + CODE-074 (ANALYTICS_DASHBOARDS.md - funnels, cohortes hebdo, 5 alertes Slack, 3 dashboards PostHog). 74% (74/100) |
| 2026-06-05 | CODE-079 | CODE-079 | **Phase O 1/5 démarrée** : RGPD — migration SQL (table deletion_tokens + 3 fonctions SECURITY DEFINER : fn_cp_export_user_data/fn_cp_request_deletion_token/fn_cp_delete_user_data) + edge function export_user_data (JSON download RGPD Art.20) + edge function delete_user_data (2-step request/confirm + auth.admin.deleteUser) + CpPrivacyPage Flutter (5 sections RGPD, export via share_plus, suppression 2 dialogs + code 6 chiffres) + route enregistrée. 79% (79/100) |
| 2026-06-05 | CODE-080 | CODE-081 | **Phase O 3/5** : CODE-080 (docs/cas_pratique/COOKIES_CONSENT_SPEC.md — spec banner consent Next.js/React : 3 catégories cookies strict/analytics/marketing, comportement banner+panel personnalisation, cookie copiq_consent 13 mois, architecture ConsentProvider+Banner+Panel+consentStorage, design brand #1147D9/#000B36/Montserrat, a11y WCAG AA, 8 tests, TODO prod) + CODE-081 (docs/cas_pratique/A11Y_AUDIT.md — audit WCAG 2.1 AA complet : 7 domaines, 20+ critères perceptible/utilisable/compréhensible/robuste, 10 flows VoiceOver/TalkBack, plan P0/P1/P2, pattern reduceMotion). 81% (81/100) |
| 2026-06-06 | CODE-092 | CODE-092 | **Phase Q 4/5** : CODE-092 — Recherche full-text avancée. Migration `20260606000001` (french_unaccent config, search_vector tsvector, trigger MAJ auto, 5 indexes GIN/trigram, vue dénormalisée, RPCs cp_search_cases_fts+autocomplete). `CpSearchService` singleton + `CpTextHighlighter`. `CpAdvancedSearchBar` overlay autocomplete + chip Non-fait. `CpHighlightedText` RichText spans. `CasPratiqueFilters.notDone` ajouté. Liste page bifurque FTS vs repo selon contexte. `CpTokens.brand`+`brandFor` ajoutés. 92% (92/100) |
| 2026-06-06 | CODE-093 | CODE-093 | **Phase Q 5/5 FINIE** : CODE-093 — Notifications push intelligentes FCM. Migration `20260606100000` (table cp_user_notification_prefs : 4 booleans topic + quiet hours + timezone + fcm_token, RLS propriétaire, trigger updated_at, vue admin service_role). Enum `CpNotifTopic` (4 topics FCM : cp_new_case/cp_streak_risk/cp_appeal_result/cp_leaderboard). Service `CpPushService` singleton (subscribeToTopic/unsubscribeFromTopic, opt-in granulaire SP+Supabase, quiet hours chevauchement minuit, onMessage foreground supprimé si quiet, onTokenRefresh stream, onUserSignedIn/SignedOut). Page `CpNotifPrefsPage` (4 toggles topic + sélecteur quiet hours showTimePicker 24h, dark/light Montserrat). Route `/cp/notifications/prefs` enregistrée. 93% (93/100) |
| 2026-06-06 | CODE-094 | CODE-094 | **Phase R 1/3** : CODE-094 — Panel admin Flutter Web complet. Nouveau projet `admin/` : pubspec.yaml (supabase_flutter/go_router/fl_chart/google_fonts/flutter_markdown/timeago). AdminTokens design system (#1147D9/#000B36/Montserrat, dark+light ThemeData). GoRouter ShellRoute (login guard, /admin/* routes). AdminShellScreen NavigationRail 220px (6 destinations + dark toggle + logout). AdminService singleton Supabase (auth, KPIs, cas CRUD, questions, rubric, synonymes, appels, audit log). Écrans : DashboardScreen (KPI grid 6 cards, line chart fl_chart 30j, top 5 failed, quick actions), CasesScreen (table filtres/tri/bulk/status-badge), CaseEditScreen (4 onglets : Infos/Situation markdown/Questions+Rubric/Preview), AppealsScreen (cartes enrichies + dialogs Approuver/Rejeter), SynonymsScreen (table + chip editor), AuditScreen (timeline diff JSON collapsable), UsersScreen (lecture seule). Migration SQL `20260606200000` : 5 RPCs SECURITY DEFINER (cp_admin_kpis, cp_admin_daily_attempts, cp_admin_top_failed, cp_admin_synonyms, cp_admin_appeals). 94% (94/100) |
| 2026-06-07 | CODE-095 + CODE-096 | CODE-095 + CODE-096 | **Phase R 3/3 FINIE** : CODE-095 (multi-tenant orgs : 3 tables SQL + trigger + 6 policies RLS + fn_cp_is_trainer + 2 RPCs admin. 4 modèles Dart OrgPlan/OrgRole/AdminOrganization/AdminOrgMember/OrgPromoStats/OrgWeakTheme. AdminService +9 méthodes. organizations_screen.dart : CRUD orgs + members sheet. Route /admin/organizations. Nav item ajouté.) + CODE-096 (stats formateur : 2 vues SQL cp_org_learner_progress+cp_org_kpis, 4 RPCs SECURITY DEFINER cp_trainer_promo_stats+weak_themes+export_csv+admin_member_stats. org_stats_screen.dart : onglets classement DataTable rang/email/promo/cas/moy%/meilleur/date + thèmes faibles barres couleur + KPI row 4 tuiles + export CSV presse-papier.) 96% (96/100) |
| 2026-06-06 | CODE-097 | CODE-097 | **Phase S 1/4** : CODE-097 — CI/CD complet. flutter-ci.yml (156 lignes, 4 jobs : analyze+format / tests+coverage+Codecov / build-android debug / build-ios debug, concurrency cancel). flutter-release.yml (336 lignes, 6 jobs séquencés : build-android signé AAB+APK keystore / build-ios IPA Fastlane Match / sentry-sourcemaps split-debug-info+obfuscate+sentry-cli / deploy Play Store internal / deploy TestFlight / GitHub Release changelog auto). fastlane/Fastfile (204 lignes, lanes android+ios+error). fastlane/Appfile + Gemfile. CICD_SECRETS_GUIDE.md (154 lignes, 15 secrets, keytool snippet, Fastlane Match init, procédure release 3 commandes). 97% (97/100) |
| 2026-06-06 | CODE-098 + CODE-099 + CODE-100 | CODE-098 + CODE-099 + CODE-100 | **🎉 PHASE S 4/4 FINIE — 100/100 TÂCHES LIVRÉES** : CODE-098 (force update : migration cp_app_version_config + edge fn app_minimum_version + AppVersionChecker service + ForceUpdateScreen + AppVersionGate, wiring documenté). CODE-099 (10 flows Maestro E2E : signup/onboarding/premier_cas/correction/appeal/paywall/achat/leaderboard/partage/suppression_compte, job e2e-maestro dans flutter-ci.yml sur push main). CODE-100 (RELEASE_V1_CHECKLIST.md : 50 items en 7 domaines, procédure release, tableau décisions post-launch). 100% (100/100) |
